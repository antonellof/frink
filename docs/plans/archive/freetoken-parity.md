---
name: FreeToken feature parity, what the port left, and in what order
overview: "GOAL: close the distance between `frink-edge` (the FreeToken policy port that landed on branch `claude/freetoken-rust-conversion-23u6ng`) and FreeToken's actual feature set. WHAT ALREADY LANDED: ~7,700 lines of Rust, 193 tests, covering the host-side DECISION logic -- radix prefix caches (plain/SWA/hybrid), the q* bandwidth split, the global expert LRU, pool budgets, admission arithmetic, and the reasoning/tool-call parsers. Only the two parsers and the stop-string withhold rule are wired into anything. THE BLOCKER THAT RANKS EVERYTHING BELOW: `frink-moe/src/lib.rs:878` says in its own doc comment that `run_expert_placed` 're-uploads each weight matrix to the device from scratch' on every call. There is no persistent GPU expert residency, and there is no CPU MoE path that can run concurrently with a device copy. Without BOTH, the q* premise -- that a step costs max(fetch, cpu) rather than fetch + cpu -- is arithmetically false on this engine, and `frink-edge::qstar` is a correct policy with nothing to decide. So Phase C is not 'the hard part to do later', it is the part that makes Phase C's own policy mean anything, and everything in Phase A is ranked ahead of it precisely because Phase A pays off on CPU and Metal TODAY. HONESTY NOTE ON SCOPE: FreeToken is CUDA-only (RTX 30/40/50). Frink holds CUDA to 'must compile' with no pinned benchmark host and no published timings (docs/FEATURES.md). Full parity therefore means building a CUDA path frink has never had receipts for, on hardware nobody has run this engine on. Phase C should not start until parity-scope-decision is answered in writing. NOT A GOAL: porting FreeToken's Triton/CUDA kernels, its C++ CPU-MoE extension, or its torch model stacks -- frink has its own, and a line-by-line port of those is a rewrite of frink, not a port into it."
todos:
  - id: dedup-sweep-2026-08
    content: "A DEDUPLICATION SWEEP, run because the reader asked whether the three `bench*` commands overlapped. They do NOT -- `bench` measures frink's kernels against llama-bench, `serve-bench` measures the server under concurrency, `bench-bw` measures the MACHINE and runs no model at all, writing the profile qstar reads. But the question was worth asking, because a scan for duplicated function bodies across the workspace found FOUR clusters, and TWO of them were hiding real defects. (1) `quant_kind_for`, copied into six GGUF loaders and drifted into two variants -- 21 dtypes vs 17, missing IQ1_S / IQ2_XXS / IQ3_XXS / MXFP4. An unmapped dtype is `LoadError::UnsupportedDtype`, not a slow path, so a DeepSeek-MLA checkpoint at IQ2_XXS was REFUSED OUTRIGHT while the identical quant loaded on the generic path. Now one table in frink-core beside the GPU kernel tables, whose own doc already said why: 'Duplicating this list is how IQ4_XS batched prefill silently ran on the CPU.' (2) `load_weight_matrix` / `load_f32_vec` / `find_info` / `split_expert_tensor`, same six loaders, TWO defects. The dtype gap again -- `loader.rs` widened twenty dtypes, the five architecture loaders three, so a Q8_0 norm or bias failed to load on five of six architectures. And a Metal one that fails silently: only `loader.rs` called `register_weight_mmap`, so on Metal every weight of every non-generic architecture was COPIED into its own MTLBuffer instead of aliasing the mapping, and paid a 64-page content fingerprint on every cache lookup on the decode hot path. Correct output, quietly worse memory and speed, on five of six architectures. 475 lines removed. (3) Three copies of the CLI's HTTP client (`chat` buffered, `chat` streaming, `serve-bench` streaming) with `parse_url` duplicated character for character -- and already drifted: one set both timeouts and the other only the read timeout, and only one header-skip loop broke on EOF, so the other would SPIN on a truncated response. One `http` module, 153 lines removed, verified live against a running server. (4) `map_tool_event` / `with_keepalive` / `token_text`. The streaming pair could not share a TYPE -- the two `GenEvent` enums genuinely differ -- so a small `StreamEvent` trait carries the RULE instead: an empty text run or argument fragment must produce NO event, because a zero-length content delta tells a client the model is producing text when it is not. DELIBERATELY NOT MERGED, and recorded so nobody re-litigates it: `print_available_devices`, duplicated between CLI and server. No crate both depend on carries the CUDA and Metal capability probes unconditionally, and making frink-core do so would turn two OPTIONAL backend dependencies into required ones -- a worse trade than fifteen lines of println. The lesson worth keeping: every duplicate found here had already drifted, and two had drifted into user-visible defects. A duplicated table is not a style problem"
    status: completed
  - id: pending-status-sweep
    content: "A RECORD OF VERIFICATION, so the remaining pending items are not re-checked one at a time. Three items -- prefill-double-buffer-bookkeeping, gdn-chunk-track-checkpoint and swa-hybrid-commit -- were marked pending after they had already landed, on top of the fifteen an earlier sweep caught; each was then verified rule by rule against the tree and against a named test rather than re-labelled on faith. Every OTHER pending item was swept for an implementation in the same pass and is genuinely open: `block_sparse` in the tree is a Kimi tensor-name prefix and not MiniMax-M3 block-sparse attention; `supervisor` appears only in doc comments (several of them written by this plan's own work); and gemma4-moe-router, dsv4-per-layer-compress-schedule, df11-lossless-codec, child-log-stream, shell-tui, paged-decode-path, persistent-gpu-expert-cache, bench-bw, double-buffered-prefill, expert-bank-quant-formats and ftw-format have no implementation at all. The lesson worth keeping: a large landing makes plan statuses lag, and a status is only worth what the last verification against the tree was worth"
    status: completed
  - id: publish-order-needs-frink-edge
    content: "DONE 2026-08-24. `frink-edge` is in .github/workflows/publish-crates.yml's CRATES list, placed with the leaves (after safetensors, before cuda) because it has no internal dependencies of its own while cuda, models, server, cli and inference all need it. The ordering comment above the list now says why the order is not alphabetical, so the next person adding an internal dependency sees the constraint. This was not tidy, it was load-bearing: v0.11.0 built its tarballs and never reached crates.io because frink-cli had gained a dependency on frink-server and the list still published cli first. The same shape would have happened here."
    status: completed
  - id: sweep-2026-08-recursive-review
    content: "A full recursive review of the FreeToken tree (435 Python files, six parallel readers, one per subtree) against every frink crate, run because the first pass was scoped by the plan rather than by the source. It found 34 gaps the plan did not have, listed below as their own todos, and confirmed the exclusions already recorded in docs/THIRD_PARTY_NOTICES.md. ONE OF THEM IS A CORRECTNESS BUG IN CODE ALREADY SHIPPED (noaux-tc-group-limited-routing). The review also confirmed a large PRESENT list -- the radix family, the q* split, the expert LRU, the parsers, effort, detokenize, scheduler, pool arithmetic and cache_report are all matched symbol for symbol. Acceptance: this todo is the index; each finding below closes on its own"
    status: completed
  - id: noaux-tc-group-limited-routing
    content: "DONE, and it was a real bug in shipped code. `route_top_k_grouped` took k from every group and truncated globally; the rule is to score each group by the SUM OF ITS TOP-2 biased scores, keep the `topk_group` best, mask the rest to -inf, run ONE global top-k, and gather weights from the UNBIASED scores. The test that asserted the old behaviour was testing the bug and is replaced by two that pin the real rule -- `group_limited_routing_concentrates_into_the_surviving_groups`, which fails against the previous implementation, and `a_group_is_scored_by_its_top_two_not_by_its_best_member`, which separates the top-2 sum from the best single member"
    status: completed
  - id: chat-default-max-tokens
    content: "DONE. `DEFAULT_CHAT_MAX_TOKENS = 32_768` (crates/frink-server/src/lib.rs), and `max_tokens: 0` is a 400 rather than a silent empty answer. Original gap: `default_max_tokens()` is 16 (crates/frink-server/src/lib.rs:1045, and again in openai_extra.rs:134) -- OpenAI's legacy completion floor, which FreeToken deliberately keeps out of its chat and Responses paths (`generation.py:154 DEFAULT_MAX_OUTPUT_TOKENS = 32768`, and responses_api.py:183 says so in as many words). A chat client that omits `max_tokens` gets 16 tokens of answer. Separately `max_tokens: 0` is currently ACCEPTED and silently produces nothing; upstream makes `max_tokens is not None and < 1` a 400, because an explicit zero must not read as unset and a zero-token budget request never becomes decodable. Acceptance: an omitted budget yields a full answer, an explicit 0 is a 400"
    status: completed
  - id: admission-context-clamp
    content: "DONE. `ContextCeiling::limit`/`prompt_refusal` (crates/frink-server/src/budget.rs) and the CLAMP in generate.rs: a prompt that does not fit is refused by name, a budget that overruns the window is trimmed to what is left rather than the whole request being rejected. Original gap: Two outcomes, not one (python/freetoken/scheduler/scheduler.py:492-522). If `input_len >= max_seq_len` the request is REFUSED, phrased exactly `prompt is too long: {n} tokens > {m} maximum` because Claude Code and OpenClaw match on that text and the Anthropic wire carries no error code. Otherwise `max_tokens` is CLAMPED to `max_seq_len - input_len` and the request is served. Frink refuses the combined case (crates/frink-server/src/budget.rs:110) and never clamps, so a servable 100k-prompt request becomes a 400. Gated with chat-default-max-tokens: a 32768 default is only safe once the clamp exists. Acceptance: a prompt that fits with a smaller budget is served rather than refused"
    status: completed
  - id: thinking-disable-path
    content: "DONE. `ThinkingSwitch`, `THINKING_KWARG_KEYS`, `DISABLE_EFFORTS` and `thinking_direction()` in crates/frink-server/src/lib.rs; `resolve_template_kwargs` has the OFF branch and short-circuits when the caller steered explicitly. Original gap: There is no wire path that turns a reasoning model OFF. `resolve_template_kwargs` only ever adds the ON direction (crates/frink-server/src/lib.rs:1120); `frink_edge::effort::thinking_off_kwargs` and `thinking_adaptive_kwargs` exist and are called from nowhere. Three rules (python/freetoken/server/model_meta.py:420, openai_api.py:47): a caller who already set ANY of `enable_thinking`/`thinking`/`thinking_mode`/`reasoning_effort` in `chat_template_kwargs` wins wholesale and the protocol knob is ignored; `reasoning_effort` of `none`/`off` means DISABLE and broadcasts the off pair -- it is not a gear to quantize; and the DeepSeek-wire `thinking: {type: enabled|disabled}` decides outright, `disabled` beating any effort, with an unknown type a 400. Acceptance: a request can turn thinking off on a checkpoint whose template supports it"
    status: completed
  - id: prerender-template-check
    content: "DONE, and it turned out to be already satisfied by the fallible render that landed with chat-template-kwargs-plumbing: `chat_completions_stream` renders through `prompt_from_messages(...)?` BEFORE it constructs the SSE response, so a template that rejects this conversation is an ordinary 400 with the compiler's own message rather than an in-stream error chunk on an already-200 response -- which several agents render as an empty reply. It was an unpinned property, so it is now a test: `a_template_that_rejects_the_conversation_is_a_400_on_the_streaming_path` asserts both directions, the refusal AND that the same template still serves a conversation it accepts, so a template that refused everything could not pass it. It fails if the render moves back inside the spawned generation task"
    status: completed
  - id: sse-keepalive-data-frame
    content: "DONE, on all four streams. `crate::sse::with_keepalive` + `keepalive_event` replace `Sse::keep_alive(KeepAlive::default())` on the chat stream: after 15s of silence it sends a real `chat.completion.chunk` with an EMPTY delta, which a concatenating client adds nothing from and a stream-idle timeout does reset on. Silence before the first event counts, which is the point -- that window is the queue wait plus the prefill, the longest quiet stretch of a long request, and a keepalive armed only after the first token would miss exactly the case it exists for. Keepalives are injected outside the `resume::Emitter`, so a replayed stream never re-delivers a keepalive that was only ever about a connection that no longer exists. The replay stream (resume.rs) counts three quiet `RESUME_WAIT` polls -- the same 15s, in polls rather than off a clock, so it is deterministic -- and sends an id-less chunk with `choices: []` (the usage-only chunk's own shape), which asserts nothing about an answer that has not started and does not enter the sequence a `Last-Event-ID` resumes from. `/v1/responses` and `/v1/messages` already had protocol-native data keepalives (`response.in_progress`, `ping`)"
    status: completed
  - id: matched-stop-reporting
    content: "DONE. `StopStep::Matched` now carries `{ text, stop }` and `FinishReason` has a `StopSequence(String)` variant beside `Stop`, so which fence fired survives to the wire. `earliest_stop_match` returns the matching stop with the cut, leftmost first and longest-on-a-tie -- without a rule the reported stop would depend on the order the caller happened to list them. `/v1/messages` reports `stop_reason: \"stop_sequence\"` WITH `stop_sequence: <the string>`, never one without the other, on the buffered body and in the terminal `message_delta` alike. Two filters keep it honest: only a stop the CLIENT sent in `stop_sequences` qualifies (`caller_stop`), because the served template adds its own end-of-turn marker and reporting that would tell an agent it hit a fence it never put up; and truncation outranks everything while calls outrank a stop, so a client is never told an answer ended on a fence when it ended on the budget. The OpenAI surface still answers `\"stop\"` -- its vocabulary has no other value, and inventing one would make a completed answer look like a failure. `FinishReason` gave up `Copy` for this, which cost two match arms and nothing else"
    status: completed
  - id: cached-tokens-report-shape
    content: "DONE. `Usage::cached_tokens` is `None` when no prefix cache is configured and `Some(0)` when one was consulted and missed, and every surface treats both as 'nothing was served from cache' rather than rendering a zero row as a cache that exists. Original gap: Two rules, both wrong today. (1) The prefix-cache hit is a SIDE CHANNEL about other users' prompts on a shared server, so upstream reports it only under an explicit flag, off by default; frink reports it whenever a prefix cache exists (crates/frink-server/src/generate.rs:562) with no opt-out. (2) The wire shape is `usage.prompt_tokens_details.cached_tokens`, and the `prompt_tokens_details` object appears ONLY for a non-zero hit so a disabled report and a genuine zero serialize identically; frink emits a flat top-level `cached_tokens` (crates/frink-api/src/usage.rs:53) that OpenAI clients do not read. Anthropic inverts the accounting: `input_tokens` EXCLUDES the cached prefix and `cache_read_input_tokens` carries it. Acceptance: the documented shape, behind a flag"
    status: completed
  - id: assistant-reasoning-replay
    content: "DONE. `ChatMessage::reasoning_content`, accepted on the wire under both `reasoning_content` and `reasoning` (serde alias) so a turn can be replayed in the shape whichever surface handed it back, and emitted to the template under all THREE keys families read -- `reasoning_content` for DeepSeek/GLM, `reasoning` for Qwen, `thinking` for harmony -- since a template treats a key it does not know as undefined, and emitting one drops the replay on every checkpoint written against another. The gpt-oss exception is in and is a RAISE, not a bad render: harmony rejects an assistant turn that makes tool calls while carrying both visible text and `thinking`, so `thinking` alone is withheld in exactly that case and visible text wins. The null-content rule needs no special case here -- `message_json` already emits `content` as a string on every branch, never null. Empty is skipped like absent, so a template that opens its markers on `if message.reasoning_content` does not wrap them around nothing. Both `responses::Turn::lower` and anthropic's converter now populate it instead of documenting why they cannot; a turn that held ONLY reasoning still lowers to nothing, because there is no message for it to hang off"
    status: completed
  - id: legacy-completions-field-rejection
    content: "DONE. `/v1/completions` deserialized six fields and serde dropped the rest silently -- including `stop`, which was passed as an empty Vec unconditionally. That is the dangerous one: the caller believes generation halts at their sentinel and instead gets the full budget of text past it. `stop` is now honoured, and token-id prompts, `logprobs`, `echo`, `suffix`, `logit_bias` and any non-text `response_format` are each refused BY NAME -- declared purely in order to be refused, because a silently dropped field is indistinguishable from an honoured one. A token-id prompt is a real OpenAI shape, so `prompt` deserializes as a `Value` and says so, rather than dying as a serde type error the caller has to guess at. STILL OWED from the reference: `stream` for a single prompt, `n` prompts as a list, and `stream_options.include_usage`"
    status: completed
  - id: responses-api
    content: "DONE. `crates/frink-server/src/responses.rs`, wired at `/v1/responses` plus the two stateless 404 routes -- which exist rather than 404-ing from the router, because the two say different things: the route EXISTS and keeps nothing, which tells a client to stop polling rather than to check its base URL. All four load-bearing rules are in: `developer` folds to `system` and every system text merges into ONE leading message; `_merge_assistant_run` coalesces a replayed reasoning + message + function_call run into ONE assistant turn, so a replay does not show the model phantom turns; a truncated turn emits `response.incomplete` with `max_output_tokens` and marks only the FINAL message item incomplete; an error emits `response.failed` carrying `error.code`, which codex reads to recognise a blown context window. The keepalive is a DATA frame (`response.in_progress`), never an SSE comment, because codex's stream-idle timeout only resets on data"
    status: completed
  - id: anthropic-content-conversion
    content: "DONE. `crates/frink-server/src/anthropic.rs`. Every rule is in: top-level `system` and every system-role message merge into ONE leading turn; `tool_use` becomes `tool_calls` with JSON-encoded arguments; a `tool_result` on a user turn becomes a `role: tool` message keyed by `tool_use_id` (falling back to `id` only when absent) and emitted IN PLACE, so a result-plus-question turn keeps its order; `redacted_thinking`, `image` and unknown blocks are skipped, not 422'd; `tool_choice: none` empties both tool lists while a named tool narrows only the template list. The conversion produces a real `ChatCompletionRequest`, so template kwargs, thinking resolution, the stop set and sampling all come from the shared chat machinery rather than a second copy. A `thinking` block becomes `reasoning_content` and reaches the template (see assistant-reasoning-replay); a turn holding only thinking still contributes no message, because there is nothing for the reasoning to hang off"
    status: completed
  - id: anthropic-sse-stream
    content: "DONE. `GenEvent` -> `MessagesStream` -> `Frame`, the same testable split responses.rs uses, driven from tests with no model and no socket. `message_start`, blocks opened and closed with an index that advances only on `content_block_stop`, thinking blocks closing with an empty `signature_delta`, tool blocks streaming `input_json_delta` and topping up the remainder against the authoritative arguments at close, terminal `message_delta` plus `message_stop`, and NO `[DONE]`. The keepalive is a protocol-native `ping` every 15 s, not axum's SSE comment, which never reaches a client's event handler. Usage excludes the cached prefix and reports `cache_read_input_tokens` only when non-zero. A caller's stop string arrives as `stop_reason: stop_sequence` with the string beside it (see matched-stop-reporting)"
    status: completed
  - id: anthropic-count-tokens
    content: "DONE. `POST /v1/messages/count_tokens`, `frink_api::routes::V1_MESSAGES_COUNT_TOKENS`, behind the same key as `/v1/messages`. It runs the same converter, the same tool lists and the same `chat_template_kwargs` and then renders with the served checkpoint's own template, so the count is the prompt a generation of that body would actually tokenize. Classification is separate as upstream has it: empty messages 400; non-empty but nothing survived conversion 400 `no tokenizable content`; a template that rejects the conversation 400; no model loaded 503. No 500 arm -- frink has no tokenizer-initialization state to fail in, and one was not fabricated. Known bias, stated in the doc comment: `Model` exposes no `bos_id`, so on a checkpoint that prepends BOS the count reads one token low"
    status: completed
  - id: tool-format-minimax-m3
    content: "DONE. `ToolCallFormat::MiniMaxM3` (crates/frink-edge/src/parser/tool_call.rs), including the longest-suffix `_partial_marker_suffix` rule the namespaced marker needs. Original gap: `ToolCallFormat::infer` routes any marker containing `minimax` to the M2 grammar (crates/frink-edge/src/parser/tool_call.rs:192), so an M3 checkpoint gets the wrong protocol -- while the M3 REASONING markers are already present, so the two halves disagree. M3 namespaces every structural tag with `]<]minimax[>[`, holds several `<invoke>` tags in one wrapper, and renders arguments as parameter-NAME elements recursively, typed from the schema at every nesting level. The rule that matters is `_partial_marker_suffix` (python/freetoken/server/function_call_parser.py:2454): the LONGEST buffer suffix that is a proper prefix of the opener, because `]` recurs inside the marker -- the generic shortest-match holdback leaks `]<` as content and breaks the marker permanently. Acceptance: an M3 checkpoint's calls parse at one character per chunk"
    status: completed
  - id: tool-format-muse-glimmer
    content: "DONE. The ATEM channel grammar in crates/frink-edge/src/parser/tool_call.rs, on both the tool and reasoning halves. Original gap: The ATEM channel grammar (`<|start|>` / `to=<recipient>` / `<|message|>` / `<|eot|>`), omitted from the first pass and recorded in docs/THIRD_PARTY_NOTICES.md as such. Rules with no analogue in frink: the templated prompt ends INSIDE an assistant channel header, so the detector must be constructible with the header already open when no reasoning parser is stacked above it; a 96-character hold window so a header split across chunks reassembles; a truncated invoke is CLOSED so the client's concatenated JSON stays valid; channel prose addressed to a non-tool recipient is dropped. Without it a muse-glimmer checkpoint's calls are printed as raw markup in the answer. Acceptance: both the tool and reasoning halves parse at three chunk widths"
    status: completed
  - id: think-gears-per-gear-kwargs
    content: "DONE. `ThinkGears` carries `kwargs` per gear (crates/frink-edge/src/effort.rs), including the always-thinking fallback: a family with a reasoning parser configured and no observable knob advertises a single `on` gear with EMPTY kwargs. Original gap: `ThinkGears` carries `supported` and `default` and nothing else, so a client knows the gear NAMES but not what to send to select one. Upstream returns a third element, `kwargs_per_gear` (python/freetoken/server/model_meta.py:360): `off` maps to the off pair, `adaptive` to the adaptive pair, an effort gear to the on pair (only when the template is toggleable) plus `reasoning_effort: <name>`. It also has a fallback frink lacks -- an always-thinking family with a reasoning parser configured but no observable knob advertises a single `on` gear with EMPTY kwargs, so a client can still label the state. Acceptance: a UI selects any advertised gear with no per-family knowledge"
    status: completed
  - id: model-sampling-defaults
    content: "DONE. `general.sampling.*` and `generation_config.json` (crates/frink-models/src/loader.rs), where an absent key stays absent rather than taking a house default. Original gap: Every request that omits `temperature` runs greedy, whatever the checkpoint's own card recommends -- a visible quality regression on reasoning checkpoints tuned for 0.6/0.95, not a tie-break (python/freetoken/utils/hf.py:92, generation.py:170). Two sources in order: the GGUF keys `general.sampling.temp`/`top_k`/`top_p`, else `generation_config.json`, where `do_sample == false` means greedy and otherwise ONLY THE KEYS PRESENT are returned -- an absent key must stay absent rather than be filled with a house default. Precedence is request, then checkpoint, then framework default. Frink hardcodes 0.0/1.0/0 (crates/frink-models/src/sampling.rs:42) and reads no such key anywhere. Acceptance: a checkpoint that recommends sampling is served with it"
    status: completed
  - id: rope-scaling-yarn
    content: "DONE. `rope.scaling.type`/`factor` are read and unknown types rejected (crates/frink-core/src/attention.rs). The `high` clamp is `rotary_dim - 1`, and the ramp is asserted against hand-computed reference values rather than against the implementation. Original gap: Found independently by two reviewers. `rope.scaling.type` and `rope.scaling.factor` are never read and never REJECTED, so a YaRN checkpoint is roped as if it had no scaling at all and degrades from the first token past its original context -- silently, because nothing in the load path notices the key. The math (python/freetoken/layers/rotary.py:139, deepseek_v4/ops.py:19): a correction range from `beta_fast=32`/`beta_slow=1` via `find_correction_dim(n) = rotary_dim * ln(orig_max_pos / (n*2pi)) / (2*ln(base))`, floor/ceil under truncate, `low` clamped at 0 and -- the load-bearing detail called out in the reference's own comment at rotary.py:176 -- `high` clamped to `rotary_dim - 1`, NOT `rotary_dim/2 - 1`, then a per-band ramp interpolating `inv_freq/factor` toward `inv_freq`. `low == high` is nudged by +0.001 rather than floored to a gap of 1. Frink's `rope_freqs.weight` path (loader.rs:475) covers Llama-3 and Phi-4, which is a DIFFERENT mechanism: a YaRN checkpoint ships no such tensor. Acceptance: the ramp asserted against hand-computed reference values, not against the implementation"
    status: completed
  - id: gdn-unequal-kv-heads
    content: "DONE. The `[num_v_heads, dk, dv]` state layout in crates/frink-models/src/gdn.rs, which unblocks the real Qwen3.5 / Qwen3-Next geometry the loader was refusing. Original gap: `crates/frink-models/src/hybrid_gguf_loader.rs:264` refuses a GDN layer when `ssm.group_count != ssm.time_step_rank` -- an honest refusal that blocks exactly the real Qwen3.5 / Qwen3-Next geometry. Gated DeltaNet is GQA-shaped: fewer K heads than V heads, and K and V head dims may differ. Each K head's q/k pair is replicated `num_v_heads / num_k_heads` before the recurrence and the state is `[num_v_heads, key_head_dim, value_head_dim]`, not square. `GdnConfig` carries one head count and one head dim and `qkv_dim()` hardcodes `3 * num_v_heads * head_dim` (gdn.rs:35,45), so the fused projection split offsets are wrong and every head reads the wrong slice. Acceptance: the unequal geometry loads and the equal case stays bit-identical"
    status: completed
  - id: gemma4-moe-router
    content: "ROUTER DONE AND TESTED; LOADER NOW REFUSES BY NAME INSTEAD OF BY A MISSING TENSOR; FULL MoE SUPPORT STILL OPEN, and the split is deliberate rather than a stopping point. THE ROUTER: `frink_moe::route_gemma4_moe` and `gemma4_router_logits`, 6 tests. Three of its four differences change the numbers without changing any shape -- a weightless router norm; a scale by a learned vector AND by `hidden^-0.5`, which leaves the top-k selection identical and moves only the softmax temperature, so omitting it is invisible; a softmax over the selected k only (sums to one, unlike a slice of a full softmax); and a `per_expert_scale[expert_id]` on each WEIGHT, after which the weights deliberately no longer sum to one, pinned by a test because renormalizing to 'fix' it would cancel the scale exactly. THE REPORTED BUG IS FIXED, though not the way the entry assumed: the symptom was a bare `TensorNotFound(blk.0.ffn_gate.weight)`, which reads like a corrupt file. A MoE layer simply HAS no `ffn_gate.weight` -- its feed-forward is `ffn_gate_exps` and friends. `refuse_moe_layer` now runs FIRST in `load_layer`, before a single tensor is read, so the error is about the architecture rather than whichever tensor happened to be missing first, and it names what the file is (expert count, top-k, whether the expert tensor is present), points at the routing that IS implemented, and says exactly what is still needed. WHY IT REFUSES RATHER THAN FALLING BACK TO DENSE: this entry's own warning -- dropping the per-expert scale or collapsing the three norms changes NO shape, so a dense fallback would load, run at full speed, and return fluent wrong text. Same pattern as `minimax_engine`'s fail-closed stub and the Granite/MiniCPM multiplier check. WHAT IS GENUINELY MISSING, and why it was not guessed: the GGUF tensor NAMES for Gemma-4's two scale vectors and its three feed-forward norms. The standard MoE names (`ffn_gate_inp`, `ffn_{gate,up,down}_exps`, `expert_count`, `expert_used_count`) are GGUF convention and are used by the detection; the Gemma-4-specific ones are not derivable from the Python source the router math came from, and inventing them is precisely how a plausible-but-wrong loader gets written. The error message asks for them, which is how they get settled the first time a real checkpoint is pointed at it. 2 tests: a synthetic MoE GGUF (attention side included, so the refusal is proven to be about the feed-forward rather than an incomplete fixture) is refused with the right error kind and content, and a dense checkpoint passes the check untouched. Acceptance still names a real MoE Gemma-4 GGUF producing reference-matching logits, which needs those names and the checkpoint"
    status: pending
  - id: dsv4-per-layer-compress-schedule
    content: "DONE. Three pieces. (1) THE SCHEDULE, in `frink_edge::dsv4::LayerCompressor`, deliberately placed beside the `compress_ratios` array the four KV tiers are already priced from -- one array, so what a layer is SIZED for and what it EXECUTES cannot drift, which a scalar on the execution side guarantees they would. A ratio is not a knob with a range: it SELECTS one of three mechanisms, so every downstream parameter is derived rather than configured -- `has_indexer` (CSA only: on an HCA layer every compressed entry is already visible, so a top-k selector answers a question with a fixed answer, at the cost of its own compressor, keys and device tier), `projection_width_multiple` (2 for CSA), `overlapping`, and `visible_compressed(pos) = (pos+1)/ratio` whose `+1` is load-bearing -- without it the query at the last position of a block cannot see the block it just completed, short by exactly one entry for the whole sequence. An unknown ratio is REFUSED naming its layer, never rounded: there is no nearest mechanism. 6 tests, one of which pins that the ring table and the compressor table agree on which ratios exist, so a layer cannot be sized without being executable or the reverse. (2) THE PER-HEAD ATTENTION SINK, which DSV4 carries and the MLA path did not have. Rather than a second copy of the softmax, `causal_mla_attention` and `causal_mla_attention_sparse` were routed through ONE body taking optional visibility and optional sinks, with `causal_mla_attention_sinks` / `causal_mla_attention_sparse_sinks` added -- a second copy is how one of four forms quietly stops matching the others. The sink joins the denominator with a ZERO value vector, so it takes mass away from the real keys instead of redistributing it, and it is NOT scaled by 1/sqrt(head_dim) (a learned logit already in score space; scaling it would make one checkpoint sink differently at different head widths, which a test now pins). A regression caught in the refactor and removed: materializing the dense path's position list allocated one usize per cached token on every decode step of every layer. 4 tests. (3) THE DISPATCH in `deepseek_v4_decoder`, three arms. `None` runs the raw window with no compressed tier -- a real schedule entry, not a disabled state, so it executes rather than being skipped, and a test pins that it never builds an entry at any length. `Hca` is the existing non-overlapping path at its real ratio. `Csa` assembles the `2*ratio`-row overlapping block: the PREVIOUS half-block through its head-role projection concatenated with the CURRENT half-block through its tail-role projection, with block 0's missing predecessor padded by zero-KV / `-inf`-score phantom rows per `dsv4_append_zero_row` -- present in the block and absent from the result, which is NOT the same as shortening it, since the per-channel softmax normalizes over whatever rows it is given. The overlap is verified by construction rather than asserted: changing ONLY the first half-block's tokens changes the second compressed entry, which a non-overlapping block could not do. An off-by-one found by that test and fixed: `n_raw / ratio` counts blocks INCLUDING the one being built, so the block's own tokens are the last `ratio` appended, not the next. TWO LIMITS NAMED RATHER THAN HIDDEN: the indexer keys project the already-compressed entry where upstream runs a separate `indexer_comp_*` compressor over raw indexer projections, and block assembly is single-head (asserted, because more heads would pool across them into one entry and leave n_compressed fractional -- a pre-existing property of this skeleton, now stated instead of silent). frink-models gained a frink-edge dependency for the schedule; edge is a leaf, so no cycle, and the publish position is unchanged"
    status: completed
  - id: minimax-m3-block-sparse
    content: "SELECTION DONE, ENGINE STILL A STUB, and the stub now says which half is which. `frink_core::block_sparse` (10 tests) is the decision half: which 128-token KV blocks a query may read. FOUR RULES, and one is load-bearing in a way the others are not -- the first `init_blocks` and the newest `local_blocks` are force-included BEFORE any scoring, and that is what guarantees the selection is NEVER EMPTY. A query early in a sequence, or one whose scores are all equally poor, would otherwise select zero blocks, and attention over zero positions is a softmax over an empty set: a NaN that propagates through the whole forward pass and surfaces as garbage output rather than as an error. A test sweeps positions with deliberately unscoreable keys to pin exactly that. The other three: a block scores as the MAX over its positions, not the mean (one strongly-related token among 127 is what sparse attention exists to catch, and a mean averages it away); NO softmax scale, because only the ORDER is consumed; and selection is PER KV HEAD with no cross-head reduction, so two heads looking for different things get different blocks -- reducing first would hand every GQA group one shared set, the opposite of the point. Ties break toward the lower block index so a cached prefix cannot disagree with the run that produced it. `MINIMAX_BLOCK_SIZE` is 128 and pins the KV page size, documented as an ABI rather than a knob. THE STUB'S MESSAGE NOW NAMES BOTH HALVES: `MinimaxEngine::reject` used to say only 'not yet implemented', which invites the next reader to re-port a selection rule that is already here and already covered; it now says the selection is ported and tested and that the loader, the 256-expert sigmoid MoE and the MTP draft heads are what is missing. STILL OPEN: those three, plus the checkpoint the acceptance names"
    status: pending
  - id: df11-lossless-codec
    content: "DECIDED: WON'T DO, and the acceptance asked for exactly this -- 'a written decision first'. THE ARITHMETIC IS THE ARGUMENT. DF11 is a LOSSLESS codec for BF16 at ~10.7 bits/weight. Frink's Q8_0 is 8.5 bits/weight (32 int8 plus one fp16 scale per 32 weights) and Q6_K is ~6.6. So DF11 is LARGER than the quantization frink already ships, and what it buys for the extra bits is bit-exactness against BF16 -- not against the original weights, against BF16. THE COST IS NOT THE FORMAT, IT IS THE HOT PATH. A canonical Huffman stream is inherently serial, which is precisely why the reference needs its interleaved 256-chunk layout (chunk j owning positions {j, j+G, ...}) and a 4096-entry `(symbol << 8) | length` LUT: a whole kernel family whose only job is to undo the entropy coding before any arithmetic happens. Frink's load path is 'GGUF mmap -> keep quantized -> fused dequant+dot', where the dequant is a few shifts inside the matvec and the weights are never materialized. Inserting a Huffman decode in front of that inverts the design. And the output would be BF16, which frink widens to f32 anyway. THE ONE CASE IT WINS is a user who needs bit-exact BF16 fidelity and cannot afford 16 bits. Frink's answer there today is the F32/BF16 path, which loads and runs at 16 bits. REVISIT IF, and only if: someone shows a checkpoint where Q8_0 measurably degrades output against BF16 AND the 16-bit path does not fit the target machine. That is the only gap DF11 fills, and nobody has produced one. Closing this also closes the DF11 half of expert-bank-quant-formats"
    status: completed
  - id: cache-status-meta-producer
    content: "DONE. `frink_edge::cache_report` (`CacheGeometry` / `CachePools`), now with a caller: `GET /v1/cache/status`. Original gap: `CacheGeometry` has a complete consumer (crates/frink-edge/src/cache_report.rs) and NO producer -- nothing in the workspace constructs one, so `cache_report` drops the VRAM column entirely and a rebuild has no advertised min/max to be clamped against: the user finds the floor by being rejected. Upstream builds it from the live engine (python/freetoken/kvcache/cache_status.py): per-unit VRAM cost, the post-weights pre-pool free-VRAM baseline, per-pool floors in the units a client types, the allocated sizes, and the whole-cache ceiling. Two conventions carry real meaning and are easy to invert: floors and pool counts are USABLE, not physical -- mamba is `min_slots - 1` and `num_slots - 1`, radix-SWA is `floor + 1` and `tokens - 1`, because each pool reserves a sentinel. Every getter is best-effort and degrades to 0 rather than raising, because it runs on the readiness path"
    status: completed
  - id: windowed-kv-cost-model
    content: "DONE. `validate_rebuild`'s `fixed_bytes` (crates/frink-edge/src/pool.rs) -- its doc named a parameter its signature did not have, so a window pool priced at zero. Original gap: `validate_rebuild`'s own doc names a `fixed_bytes` parameter its signature does not have (crates/frink-edge/src/pool.rs:288-290) -- a defect in the first pass. A window model's rebuild is therefore priced as if its window pool were free. Upstream collapses the two-pool geometry into the affine `(cache_per_page, fixed_cache_size)` pair `solve_num_pages` divides by (python/freetoken/kvcache/hybrid_swa_pool.py:298), and the branching IS the content: an SWA group ALWAYS adds `8 * page_size` for the dense full-to-window mapping, which scales with the FULL pool and not the window; naive mode puts the whole window pool in `fixed`, a pinned window puts `per_token * (max(floor,pin) + 1)` there, and ratio mode SPLITS it across both terms. Fold the ratio term into `fixed` and the pool under-sizes; drop it and the first long prompt OOMs. `spec_kv_bytes_per_token` also carries an indexer-key slab term frink has no analogue of, despite glm_dsa.rs:180 maintaining exactly that cache"
    status: completed
  - id: recurrent-state-slot-pool
    content: "DONE. `StateSlotPool` (crates/frink-edge/src/state_pool.rs): slot 0 is a padding sink that is never allocatable, live slots / ping-pong track slots / tree-donated snapshots come from ONE free list, `reclaim_all_slots` is idle-only, and `copy_from` is copy-on-write on restore. Original gap: `HybridRadixCache::insert`'s `snapshot_exists` return and `HybridEvicted::mamba_slots` have no counterparty: every refused donation and every evicted snapshot LEAKS a slot, which is the failure hybrid.rs's own doc warns about. Upstream's allocator (python/freetoken/kvcache/linear_state_pool.py:32) reserves SLOT 0 as a padding sink that is never allocatable, and feeds live slots, the two ping-pong track slots and tree-donated snapshots from ONE free list so memory flows between them by demand rather than static partition. `reclaim_all_slots` is idle-only and destructive by contract, because the tree that owns donated snapshots must be discarded in the same step. `copy_from` is copy-on-write on restore: a matched snapshot is COPIED into a fresh live slot, since the tree still owns it for the next reader"
    status: completed
  - id: gdn-chunk-track-checkpoint
    content: "DONE, also found stale on re-verification. `frink_edge::state_pool::track_checkpoint`, with all three rules and the reason each wrong-looking-right alternative is wrong: `chunk = (extend_len - 1) / CHUNK_SIZE` with nothing frozen at zero, where the `- 1` is what puts the boundary STRICTLY inside the extend so a one-chunk extend is skipped; the boundary is `cached_len + chunk * CHUNK_SIZE` and NEVER the extend's end, because the end state is the one in the request's live slot and is donated at finish -- freezing there would spend a track slot duplicating that donation and leave every interior boundary uncovered; and the destination is the idle half of the ping-pong, which then flips, so a chunked prefill calling this once per chunk cannot overwrite a checkpoint before the commit that donates it has run. It shares the destination-and-flip rule with `anchor::snapshot_at_anchor` through the same `PingPong` so the chunk-driven and anchor-driven checkpoints cannot drift, and the two never race -- one runs on a prefill extend, the other on a decode step. Three tests, including that the two agree on the destination"
    status: completed
  - id: dsv4-paged-window-allocator
    content: "DONE. `FreeListAllocator` and `Dsv4WindowPool` in `crates/frink-edge/src/dsv4.rs`. Unit bases are always multiples of the page unit and handed out LIFO from the tail, so the page-base invariant the state ring depends on cannot be violated; `alloc_swa` requires whole contiguous ascending pages and preserves in-page offsets; `free_swa` asserts full page coverage and is idempotent over already-unbound pages; `state_loc` is DERIVED from the position and never stored, with `ring_size` required to divide the page so distinct pages land on disjoint ring blocks. The prefill chunk cap is `max(P, (n_win_pages - reserved) / 2 * P)`. `window_pool.rs`'s per-token FIFO stays as-is: it is the correct answer for the generic pool it ports, and this is the per-page ring beside it, not a replacement"
    status: completed
  - id: runtime-cache-rebuild
    content: "DONE for the policy, and HONEST about what frink can execute. `frink_edge::rebuild` (6 tests) holds the transaction: `EngineActivity::is_idle` is the safe point and EXCLUDES finished requests on purpose -- a finished row holds no pages, slots or graphs, so gating on it makes a resize wait for bookkeeping rather than resources, and on a quiet server the reaping may not happen until the next request arrives, so the resize blocks until the traffic it was meant to make room for shows up. `TouchedPools` is the rollback scope: a rollback restores only the pools the request NAMED, because restoring an untouched one still frees and re-allocates it, and on the KV side that trips the invalidation gate -- so an expert-cache resize would silently cost every cached prefix in the engine to undo a change the KV pool never saw. `RebuildTxn::run` distinguishes a failure BEFORE the destructive free (`RejectedIntact` -- nothing given up, engine still serving, `restore` never even called) from one AFTER it (`RolledBack` -- the old sizes go back), on a teardown flag the destructive closure sets, because a plain `Result` makes both of those `Err`. Only a FAILED ROLLBACK latches: a failed rebuild leaves an engine serving what it was, a failed rollback leaves one with no pools and no way back, and latching on the first would take a serving engine out of service over a resize that changed nothing. WHAT FRINK EXECUTES: the transaction is wired into `cache_rebuild`, but `KvBlockPool::resize` moves counters and frees NOTHING, so there is no destructive line yet and the closure never marks a teardown -- the outcome today is always `Applied` or `RejectedIntact`. The rollback and latch arms are written out rather than defaulted, so making the resize destructive is a compile error at that match first, instead of a silent 503-forever. Verified live: 64 -> 256 pages on a running server, with a sub-page target and a `moe` request each refused by name and the engine unchanged"
    status: completed
  - id: swa-hybrid-commit
    content: "DONE, verified rule by rule -- a THIRD item found stale in this sweep. `CacheManager::commit_swa` has all four SWA rules: insert at `align_down(publish_len, page_size)` passing the request's `released_window` so the tree TOMBSTONES rather than adopts slots the request already gave back; `free_locations` frees in BOTH pools; on finish the page-unaligned tail is freed whole in both (it was charged as a whole page, so a partial release leaks the slots), then `trim_head_swa` to `prompt_len - window - SWA_RETAIN_GAP` and a RE-MATCH to re-stamp the LRU, because the prompt path is otherwise the stalest entry and the first eviction victim, so the next turn's cut near the prompt end would miss; and on an unfinished commit an EXTRA `match_prefix` one window back BEFORE the real one, which forces a node boundary there so `inc_lock` -- being node-granular -- pins the trailing window alone instead of the whole chunk. `commit_hybrid` has all three hybrid rules: the frozen snapshot is donated; `inc_lock` runs BEFORE `alloc_state_slot`, because that allocation can trigger `evict_mamba` and reclaim the just-donated still-unlocked node; and on finish the shorter pending-freeze prefix is inserted FIRST, advancing the dedup free floor before the live state is published. Every one has a named test, including `a_window_commit_tombstones_the_slots_the_request_already_released`, `an_unfinished_window_commit_pins_only_the_trailing_window`, `a_hybrid_commit_locks_its_donated_node_before_allocating_the_replacement` and `a_finished_hybrid_request_donates_its_pending_freeze_before_its_live_state`"
    status: completed
  - id: pool-occupancy-telemetry
    content: "DONE, with one deliberate divergence and one finding. `cache_admin::kv_pages` computes occupancy through `frink_edge::PoolUsage::from_available(total, free + evictable)` rather than `total - free`, so the evictable-is-memory-not-occupancy rule is stated ONCE and shared by `GET /v1/cache/status` and `GET /v1/stats`, whose new `pools` block carries the three gauges in the shape a status bar renders. A pool this deployment does not have is `null`, never a zero row: 'no window pool' and 'a window pool with nothing in it' are different facts. THE FINDING: frink's evictable count is zero today and STRUCTURALLY so -- `generate` stores a prefix only `if kv_pool.is_none()`, so a shared KV pool and the prefix cache never coexist and nothing evictable can be holding a page. The old `total - free` was therefore right by accident, not by design; it is computed through `PoolUsage` anyway so that when the two are allowed to coexist the gauge is already correct instead of quietly wrong, with a test pinning the rule. THE DIVERGENCE: upstream stamps these gauges on every reply of the batch. Frink serves them on `/v1/stats` instead -- one round trip for a status bar that is already polling it -- because the reply shapes here are OpenAI's and Anthropic's, and a pool gauge on a `chat.completion` is a field no client asked for"
    status: completed
  - id: scheduler-status-line
    content: "DONE. `frink-edge`'s `StatusReporter` had held the whole policy with no caller; `batch_scheduler::worker_loop` is now that caller, and the timing subtlety is preserved where it actually lives. `admit` RETURNS a `PrefillSnapshot` counted as each prompt is accepted, not read back off the live rows: by the time this tick's forward has run, every admitted prompt's processed count has advanced to its device length, so a line built from the live rows reports `#new-token` as one per request and `#cached-token` as the whole prompt -- the upstream test's planted-wrong-values case exactly. A tick that admits nothing logs nothing, rather than an all-zero prefill line on every idle pass. Decode lines emit every `FRINK_DECODE_LOG_INTERVAL` forwards (40 by default, `0` meaning every forward, clamped to one rather than dividing by zero) and report throughput over the gap since the LAST EMITTED line, resetting the counter -- what an operator reads a decode log for is a change, and a lifetime mean is the one statistic that cannot show one. `window` and `recurrent` are passed as `None` and omitted entirely rather than printed as zeros, because this batcher serves no windowed or recurrent model and a zero row is something an operator would size against. With no block budget configured the KV gauge is an EMPTY `PoolUsage`, which `ratio()` reads as idle rather than full. The clock is the caller's, as frink-edge holds none"
    status: completed
  - id: per-layer-cache-stats
    content: "DONE, with routing-skew-report: `LayerRoutingSkew` and `per_layer` in crates/frink-edge/src/expert_cache.rs. Original gap: `ExpertCacheStats` is one global counter set; `ensure_split` already knows the layer and discards it. A single global miss rate cannot distinguish one thrashing layer from every layer uniformly slightly over budget, which are opposite fixes -- and it is the input to every per-layer placement decision, including why `auto_cpu_layers` picks the ends. Upstream keeps per-layer counters over a reset-delimited window (python/freetoken/moe/offload_cache.py:908)"
    status: completed
  - id: routing-skew-report
    content: "DONE. Per-layer expert-cache stats and `oracle_hit_at_slots` (crates/frink-edge/src/expert_cache.rs), which is what separates 'make the cache bigger' from 'this layer's routing is flat'. Original gap: Whether an observed miss rate is the cache's fault or the routing's (python/freetoken/moe/offload_cache.py:936). From a per-layer decode histogram: the working set, `experts_for_90pct`, normalized entropy, and the load-bearing one, `oracle_hit_at_slots` -- the top-C frequency mass where `C = max(1, round(cache_size / num_layers))`, i.e. the best hit rate ANY per-layer policy could reach on the observed distribution. That number is what separates make-the-cache-bigger from this-layer's-routing-is-flat-and-no-size-helps. Without it an operator tuning slot count has only the realized miss rate and no upper bound to compare it against, so flat routing reads as an undersized cache forever"
    status: completed
  - id: prefill-double-buffer-bookkeeping
    content: "DONE, and VERIFIED rule by rule rather than assumed -- this was still marked pending after it had already landed. `ExpertCache::prefetch_prefill_layer` holds all five: `buffer_id = layer % 2`; `invalidate_prefill_buffer` clears the slot map AND sets `usage = 0` on those slots, so the LRU takes them first instead of evicting real residents; hit/miss is `slot >= 2 * num_experts` and NOT `>= 0`, so a `-1` and anything the buffers own is a miss by definition -- relaxed, a prefill would gather from slots the buffer is concurrently overwriting and load the wrong expert's bytes silently; misses coalesce into contiguous id runs via `coalesce_runs`; and a bank whose per-expert row is under `SMALL_BANK_FEAT_BYTES` (256 KiB) ships its whole layer as ONE entry even with zero misses, which keeps every entry in a batch above the driver's async floor. Five dedicated tests, one per rule, including `a_small_bank_ships_its_whole_layer_even_with_no_misses` and `a_slot_inside_the_prefill_buffers_is_a_miss_however_resident_it_looks`. The STREAM half -- events, copies, overlap -- stays open under double-buffered-prefill; this is the portable bookkeeping only, which is what the item asked for"
    status: completed
  - id: host-residency-classes
    content: "DONE. `BankResidency` / `HostResidency` / `ResidencyPlan` and WSL pin-budget detection (crates/frink-edge/src/residency.rs, `FRINK_PIN_BUDGET_GB`). Original gap: Which class each layer's expert bank settles into -- pinned, locked, or pageable -- and the three invariants that follow (python/freetoken/moe/host_banks.py:39, offload_cache.py:296): a non-pinned layer must ALREADY be a CPU layer or the load hard-fails; prefill overlap is incompatible with any non-pinned layer and is REFUSED, not silently degraded; and an unpinned layer accepts ONLY the whole-layer materialize, because that path's position-equals-expert-id is the one mapping valid without a device alias. Failure is RECORDED, not assumed: a failed lock leaves the bank pageable and the ACHIEVED labels are echoed back, one pageable bank downgrading the whole layer. `auto_cpu_layers` computes which layers to give the CPU and nothing carries the label. Also missing: the pin budget it reads -- none on plain Linux, 40% of physical RAM on WSL, where WDDM caps pinning near half of RAM shared across processes (engine.py:1161)"
    status: completed
  - id: cpu-core-affinity
    content: "DONE. `physical_core_count` in crates/frink-core/src/threads.rs deduplicates `thread_siblings_list` across this process's affinity mask, because `available_parallelism` counts SMT siblings and an auto-sized pool put two workers on every physical core. Unreadable sysfs degrades to the `available_parallelism` answer rather than failing. Original gap: `crates/frink-core/src/threads.rs:76 perf_core_count` reads a macOS-only sysctl and falls back to `available_parallelism()` everywhere else -- which counts SMT SIBLINGS on Linux, so an auto-sized expert pool puts two workers on every physical core. MoE decode is memory-bandwidth-bound: siblings add no bandwidth, contend for the same load ports, and wreck a spin barrier when oversubscribed. Upstream dedupes `thread_siblings_list` within the process affinity mask, spreads an explicit count across physical-core representatives FIRST, donates the last physical core to the coordinator rather than oversubscribing, and clamps the framework's own intra-op count to what is left (python/freetoken/moe/cpu_executor.py:91,118). No code in frink calls `sched_setaffinity` at all"
    status: completed
  - id: bench-profile-lookup
    content: "DONE. `load_policy` / `usable_profile` / `load_backend_recommendation` (crates/frink-edge/src/bench_profile.rs), reading `$XDG_CACHE_HOME/frink/benchbw/<uuid>.json` with the legacy `benchbw.json` still honoured. Original gap: `BandwidthProfile` parses the document and `matches_gpu` enforces the card check, and NOTHING resolves a path or reads a file -- the type has no caller, so `policy_for` has no input on any real host and every deployment runs the unbenchmarked fixed cap. One file per GPU UUID because bandwidth differs between slots on the same box; candidate order is explicit path, then env var, then the UUID file, then the legacy one; and the sharp rule (python/freetoken/moe/bench_profile.py:99) is that a candidate that EXISTS BUT DOES NOT PARSE returns None rather than falling through, so a corrupt per-card profile never silently borrows another card's numbers"
    status: completed
  - id: serving-benchmark-client
    content: "DONE. `frink_edge::bench_client` holds the methodology with no socket in it (10 tests) and `frink serve-bench` is the socket. All four rules are in: requests pinned to temperature 0 / top-k 1 / ignore_eos / an exact output length; a POSITIONAL TTFT/TPOT split; nearest-rank percentiles over samples POOLED across requests, never per-request means percentiled afterwards; and throughput as total tokens over the whole run's span. Two rules had to be discovered rather than ported, both by running it against a live server. FIRST, what counts as a tic: `is_token_chunk` excludes the keepalive (which arrives during exactly the window TTFT measures, so timing one reports a TTFT of about the keepalive interval for a request whose first token had not arrived) AND the terminal `finish_reason` frame (which carries no token, so timing it adds one to every request's count and one spurious inter-token sample), while deliberately INCLUDING a role-only opening chunk. SECOND, chunks are not tokens: a buffered answer -- the synthetic-weights path, a continuous-batching row with no incremental stream -- arrives as ONE chunk and was still N tokens of work, so the count comes from the server's own `usage.completion_tokens` and only the timing comes from the tics. A buffered stream reports TTFT and end-to-end but no TPOT: that detail does not exist and is left absent rather than invented. This also required `ignore_eos` on the wire, which frink did not have -- honoured on BOTH decode paths, suppressing the MODEL's end-of-generation set only, since a caller running past the model's opinion about length is not a caller withdrawing their own fence. NOT ported: trace replay (`replay_offsets` is there and tested, the CLI has no trace input yet) and `prompt_of_exact_length` (there and tested; the CLI sizes prompts in characters, because an exact TOKEN length needs a round trip per fixed-point step against the server being benchmarked)"
    status: completed
  - id: engine-footprint
    content: "DONE. `frink_edge::footprint` (parsing + caching policy, 6 tests) and `cache_admin::footprint_json`, surfaced as `memory` on `GET /v1/stats` beside the pool gauges -- so an operator can finally ask whether the engine is using what `device_budget` planned, which is the one question a budget is sized to answer. PSS from `/proc/self/smaps_rollup` first, because it divides each shared page by its sharers and is therefore the only figure summable across a process group; RSS from `/proc/self/status` is the fallback for a kernel older than 4.14 and OVERCOUNTS the moment anything is shared, so WHICH quantity was read travels with the number -- a caller comparing a PSS figure with an RSS one is comparing two different things and reads the difference as a leak. `sum_footprints` propagates the overcount: a group in which any member could only be read as RSS is labelled RSS rather than silently passed off as PSS. Behind a 2s `ProbeCache`, because `smaps_rollup` walks the whole VMA list and four dashboards polling would otherwise produce four concurrent walks, each slower for the others being there -- the endpoint measuring its own instrumentation. Holding the lock ACROSS the probe is the point, not an oversight: a caller arriving mid-probe blocks and finds the fresh value. A failed probe is neither cached (which would answer 'unknown' for a whole TTL after the condition cleared) nor answered with the stale value (the caller asked what is true NOW). `null` and never zero on a host with no `/proc`: an engine using no memory is not a thing that happens. NOT ported: the process-GROUP walk -- frink-server is a single process, so `sum_footprints` exists and is tested but has one reading to sum"
    status: completed
  - id: child-log-stream
    content: "NOT BUILT, deliberately, and the reason is checked rather than assumed: frink has NO CONSUMER for it. The item describes how a DAEMON turns a loading engine CHILD's terminal output into a streamable log, and frink-server is one process with no child -- `loading_model` is a label for `/admin/models`, not a stream. Building the policy anyway would produce exactly what this plan has spent its time removing: a well-tested module with nobody to call it. The rules are worth keeping written down for the day a supervisor exists (they are genuinely subtle -- a run of carriage returns coalescing to ONE so a CRLF pair is one newline rather than a throttled partial plus a phantom blank; a bare-CR run followed by a real character being a redraw, throttled so a multi-minute weight load shows movement without flooding; a ring with an all-time sequence that survives eviction, with the SSE handler SUBSCRIBING BEFORE it snapshots the backlog and deduping by sequence so the replay-to-live boundary has neither a gap nor a duplicate; and a slow client overflowing drop-oldest with a client-VISIBLE gap sentinel rather than a silent loss). Gated on serve-supervisor-lifecycle, which the plan already records as a product-surface decision rather than a technical obstacle"
    status: pending
  - id: accounting-outbox
    content: "DONE. `frink_edge::outbox` (6 tests) plus the write in `cache_admin`. The ORDERING is enforced by construction: `finish_stop(receipt, persist, signal)` reaches `signal` only when `persist` returned `Ok`, and there is no path through it that skips the write -- reversed, a crash between the two loses a whole engine generation's totals, since the engine is gone and the only record of what it served went with it. In frink the REPLY is the signal (there is no child process), so the receipt is durable before `prepare-stop` answers and a write failure is a refusal rather than a warning: the engine is preserved, the totals are already sealed, and a retry seals the same numbers under the same id. Receipt ids are DERIVED from a stable identity (`FRINK_INSTANCE_ID`, else pid + the process's wall-clock start -- a pid alone is reused across restarts and a restarted engine reusing a previous generation's id would have its own receipt silently skipped as already written), so a supervisor whose response was lost retries into the SAME document instead of creating a second billing event. Persist is idempotent by that id and writes through a temp file plus a rename, because a plain write is not atomic and the moment a reader is most likely to look is while the engine is stopping. A snapshot whose drain did not complete is DEMOTED to `degraded` at the boundary, so every consumer sees the qualification rather than the first one that forgets billing a partial total as a whole one. FOUND WHILE VERIFYING AGAINST A LIVE SERVER: the already-sealed retry path answered without the receipt id -- exactly the caller idempotence exists for -- and now persists and reports on that path too. ONE DIVERGENCE: the id is a UUIDv8 (RFC 9562 custom) over SHA-256, not the reference's v5, because a v5 is SHA-1-based and frink depends on no SHA-1; stamping `5` on a digest that is not one would mislead anyone re-deriving the id. Configured by `FRINK_ACCOUNTING_OUTBOX`; unset means no outbox and no persistence step, since inventing a directory to write accounting documents nobody asked for is worse than not keeping them"
    status: completed
  - id: serve-supervisor-lifecycle
    content: "DONE as POLICY, on the same split every other frink-edge module uses: `frink_edge::supervisor` holds the decisions and a caller-supplied `ProcessHost` does the spawning, exactly as `expert_slots` holds residency decisions behind `SlotDevice`. The reason is not tidiness -- every rule here is a RACE, and a race you can only reproduce by starting real multi-gigabyte processes is a race nobody tests. Behind the trait they are all testable on any host, and all four were SABOTAGE-CHECKED: deleting the rule fails exactly one named test and nothing else. (1) START IS A THREE-WAY GATE. Same spec while running is an idempotent no-op, because a supervisor whose response was lost retries and the retry must not become a second engine -- asserted on the SPAWN COUNT, since the outcome value alone cannot show a second process. Different spec is a Conflict, refused rather than silently reconfiguring something a caller is using. A spawn in flight is awaited and RE-EVALUATED, which is why the already-running check lives inside `start` rather than at the call site: a second caller blocking on the gate re-runs it against the state the first left. `Supervisor` is deliberately NOT internally locked -- an inner mutex would let two callers each hold a consistent view of an inconsistent world. (2) EXACTLY ONE REAPER. `signal` and `reap` are separate trait methods, and `signal` must not block: a poll TRANSIENTLY LIES while the monitor holds the lock for its own wait, so a stop trusting it reports failure for a stop that worked. Pinned by asserting the ORDER and the counts, not just the return. (3) THE STOP-REQUESTED LATCH IS SET BEFORE THE SIGNAL, so a child dying between the two is still a requested death; a stop whose SIGNAL FAILS keeps the latch, since the signal may simply have raced the exit. Without this, stopping a serve is how you restart it. (4) SHUTDOWN IS PERMANENT and latched BEFORE the stop it performs, so a start racing shutdown is rejected rather than spawning something the stop has already walked past. RE-ADOPTION keys on (pid, start_time, argv, port); the sabotage that reduces it to pid+port fails the recycled-PID test, which is the whole reason `start_time` is in the key -- a daemon restarting after a crash that adopts 'the process at PID 4711' can adopt a shell. OOM VICTIM SELECTION scores the whole engine group positive and the daemon negative (killing the reaper first orphans the engine), and REWRITES PERIODICALLY, with a test pinning that a worker forked after the first write is covered by the next one -- the half everyone forgets, since a score written once covers only what existed when it ran. 13 tests. WHAT IS NOT BUILT is a `ProcessHost` implementation, because frink still ships one binary: that is the product surface this item always said it was, and it is now the only part left. `child-log-stream` is unblocked by this in principle but still has no consumer for the same reason"
    status: completed
  - id: shell-tui
    content: "MOSTLY DONE. The reasoning bug is fixed (see below), and the slash commands now exist, each reading the SERVER rather than a hardcoded list: `/stats` renders `/v1/stats` -- throughput, requests, the KV gauge and the memory footprint -- with any figure the server reported as null printing as `-` and NEVER as `0`, since a p95 of zero reads as an instantaneous server and a memory figure of zero as an idle one, which are exactly the shapes that endpoint avoids emitting; `/think` cycles the gears from `/v1/models`'s `supported_reasoning_efforts` and sends that gear's own `reasoning_effort_kwargs` verbatim, because which efforts exist is a property of the served checkpoint's template and a client with a fixed list offers gears the model does not grade while hiding ones it does -- cycling starts at the FIRST gear so the first press is deterministic across checkpoints, a gear this server lacks is refused by name, a family advertising one gear with EMPTY kwargs passes those through (an always-thinking model has nothing to send and that is a real answer), and nothing selected sends no key at all so the template's own default applies; `/cache` shows `/v1/cache/status` or resizes through `/v1/cache/rebuild`, relaying the server's refusal text verbatim because it names the floor a shrink must clear. An unknown `/command` is refused rather than sent to the model, which would otherwise answer with a plausible hallucination about a command that does not exist. Five tests over the pure halves; verified live end to end, including a 64 -> 128 page resize that persisted. The earlier fix: `frink chat` read only `choices[0].delta.content`, so a reasoning model's deltas were dropped and its answer looked like it arrived out of a long silence -- the thought is now printed dimmed and deliberately NOT appended to the replayed assistant turn. The PER-TURN INTERRUPT is in: SIGINT mid-generation sets a flag the stream loop reads between chunks, which cancels the generation through `/v1/cancel` using the id the SERVER stated on the first chunk (a client cannot cancel a generation it was never told about) and hands back what arrived -- so Ctrl-C costs the turn rather than the conversation. At the PROMPT it exits 130, because that is what Ctrl-C means at a shell prompt and a REPL that cannot be quit with it is worse than one with no handler. The handler does exactly two async-signal-safe things, an atomic store and `_exit`; a `println!` or an allocation in there is undefined behaviour whose failure mode is a deadlock inside the allocator that looks like a hang. A guard clears the in-flight mark however a turn ends, panic included, or the next turn would cancel itself before its first chunk. Verified live: EOF exits 0, SIGINT at the prompt exits 130. STILL OWED, all pure rendering: the always-on status footer, the 8-line visual cap with in-place redraw, and ~20Hz coalescing"
    status: completed
  - id: publish-order-blocked-on-workflow-scope
    content: "BLOCKED, AND IT BLOCKS THE RELEASE. `frink-edge` is missing from the crates.io publish order in `.github/workflows/publish-crates.yml`, and `frink-server` depends on it. That workflow fires on `push: tags: v*`, so tagging a release TODAY would publish roughly nine crates and then fail irreversibly on `frink-server`, which cannot resolve `frink-edge` at the new version -- exactly how v0.11.0 broke on frink-cli, as the workflow's own comment records, except that a crates.io version can be yanked and never replaced. The fix is three lines: add `frink-edge` to the `CRATES=(...)` list after `frink-safetensors`, and add the two comment lines placing it among the no-internal-deps leaves and noting that `server` depends on it. It CANNOT be pushed from a Claude session: GitHub refuses an OAuth app without `workflow` scope, verified by an actual rejected push. Someone with that scope must apply it, or grant the scope. Until then, DO NOT PUSH A TAG. Acceptance: the three lines are on main, then the release is cut"
    status: pending
  - id: parity-scope-decision
    content: "DECIDED: **YES**. CUDA parity with FreeToken IS a goal. Recorded 2026-08-25 by the repo owner, against the recommendation offered here (which was 'no for now' on the ledger's evidence) -- noted only so a later reader sees the call was made deliberately and not by default. Phase C is therefore OPEN, and its roughly ten items are live work rather than won't-do. WHAT THIS DECISION STILL OWES, and the item's own acceptance says so: a NAMED benchmark host. Every Phase C item needs hardware to be validated rather than merely written -- persistent-gpu-expert-cache's acceptance is a zero-upload counter on a real decode step, concurrent-cpu-moe-executor's is a measured A/B, bench-bw's is a profile a real card produced. Until a host is named, Phase C work can be WRITTEN and compiled (the repo already holds CUDA to a must-compile bar, and hardware tests stay `#[ignore]`d) but not CLOSED. Do not mark a Phase C item done on compilation alone: that is exactly the gap between 'CUDA compiles and runs' and 'nobody has benchmarked it' that docs/FEATURES.md:70 already admits to. Closed by re-decision and never by preference"
    status: completed
  - id: anchor-checkpoints
    content: "DONE as policy, NOT YET WIRED. `crates/frink-edge/src/anchor.rs`, 15 tests. All four pieces: `resolve_anchor_token` (opener -> single token id, None otherwise -- and the single-token restriction is the POINT, it makes detection an integer compare on the sampled id with no detokenization on the hot path); `AnchorState::observe` (first call of a turn only, never a terminal token); `decode_slide` (cadence + first-step overlap guard, the anchor cap, and the drop rule) with `prefill_slide` as its every-batch sibling; and `snapshot_at_anchor` for the recurrent half, whose four gates are each a real failure -- no anchor, a pending snapshot that would be overwritten, an anchor that is not the cached length (unreachable, no node ends there), an anchor that is not page-aligned (belongs to no node). SIGNED ARITHMETIC THROUGHOUT: an early request's threshold is legitimately negative and clamping it to zero before the anchor comparison makes the drop rule fire on requests that have barely started -- found by writing the short-generation test first. TWO PROPERTY TESTS carry the semantics rather than examples: an anchor may only ever hold MORE state than a plain slide (swept over 60 x 28 position/anchor pairs), and held state stays bounded by `2*(window+gap) + interval + 2*page` over a 60k-token generation, which is exactly what the `anchor_checkpoints` term in pool.rs:372 is sized for -- so that term is no longer paying for a fiction. STILL OWED: nothing calls any of it. `observe` needs the sampling loop, `decode_slide` needs a window pool to free INTO (swa-paged-allocator) and a page table to free FROM (cache-manager), and `snapshot_at_anchor` needs a recurrent-state pool frink does not have. That is the ordering this plan already has, not a surprise"
    status: completed
  - id: cache-manager
    content: "DONE for the plain-radix path, SWA/HYBRID TREE PATHS STILL OWED. `crates/frink-edge/src/cache_manager.rs`, 11 tests. `CacheManager` owns the page free list (page bases in KV-pool token coordinates), the slot-major page table, and the handover: `match_and_lock` (which excludes the LAST token from the lookup -- a prompt that matched entirely would have no position to produce logits from), `allocate` (whole pages, evicting rather than refusing while the cache still holds evictable memory), `commit` (the three-span contract: free the duplicate span, REPOINT the row at the tree's canonical pages first when the request keeps reading, free the padded tail only when finishing), `begin_batch`/`end_batch` for the deferred frees, and `slide_window`, which is what finally gives anchor-checkpoints and swa-paged-allocator a consumer. `check_integrity` is an EQUALITY over pages and is idle-only by construction, because a request in flight holds pages that are in neither place -- that is what in-flight means. TWO THINGS THE PORT MADE STRICTER THAN UPSTREAM: a failed window-slot allocation hands its KV pages straight back rather than leaving the request half-allocated (upstream allocates the two pools in sequence and leaves the caller to unwind), and every free goes through one `free_locations` that also releases window slots, so the two pools cannot drift. FOUND WHILE TESTING: allocating a long span in one call needs window slots for the WHOLE span, which is exactly why `scheduler::PrefillPass`'s chunk sizing is window-bounded -- the two pieces agree, and the test now says so. STILL OWED: `commit` speaks only to `RadixCache`. The SWA tree's path (insert with `swa_evicted`/`update_kv_after_len`, `trim_head_swa` at finish, the boundary re-match that keeps a lock to one window) and the hybrid tree's donate/dedup path are not written; both need an enum over the three caches. Neither blocks `wire-radix-prefix-cache`, which is a plain-cache consumer"
    status: completed
  - id: swa-paged-allocator
    content: "DONE as policy, NOT YET WIRED. `crates/frink-edge/src/window_pool.rs`, 11 tests. `WindowSlotPool` is the full-position -> window-slot map plus its free list: slot 0 reserved as the sentinel (so an unmapped position reads as 'no state' rather than as slot 0's bytes, and a double free is a no-op), FIFO head-take rather than LIFO (a just-freed slot is the one a sliding request is most likely to free again immediately, so LIFO would concentrate churn on a few slots), all-or-nothing allocation with the capacity check BEFORE the first slot moves, and the map over-allocated by one page because an allocation addresses whole pages and the last one can name a position a page past the final token. TWO DEPARTURES FROM UPSTREAM, both deliberate: `translate` handles a negative location explicitly instead of relying on a trailing sentinel row plus negative tensor indexing (same rule, written where a reader can see it), and `alloc` carries a `debug_assert` that the position is currently unmapped, which turns allocating over a live slot -- a leak upstream tolerates silently -- into a test failure. The conservation invariant is an EQUALITY (`available + live == capacity`) and its test proves it catches a LEAK, not only a double free: a `<=` would tolerate exactly the failure that surfaces as 'the pool is full' an hour into a long run with nothing to point at. A 2048-step sliding workload conserves after every single move. STILL OWED: the wiring, which is cache-manager's job"
    status: completed
  - id: paged-decode-path
    content: "FIXED AND VERIFIED ON HARDWARE 2026-08-27, and the server refusal came out in the same commit. THE CAUSE WAS NOT THE PAGE INDIRECTION AND NOT AN ATTENTION ARM. A Metal prefill deliberately leaves K/V on the device and fills the host `KvCache` with `advance_len` placeholders -- literal zeros, whose only job is to keep `seq_len` in step for the sync checks -- because the contiguous decode path then reads the device buffers and never the host rows. `forward_batch_last_paged` DOES read the host rows: it scatters them into the page store. So the whole prompt reached the store as zeros and decode attended over a prompt the model never saw, which is exactly the shape of `Blue ( question mark;>a> is a> is`. The narrowing in the previous note pointed at the right boundary and the wrong side of it: K and V were fine when they left the GPU, and were never asked for. FIX: `advance_host_kv_after_metal_prefill` downloads the real rows -- per layer, immediately after each launch, because the Metal KV buffers are dropped outright when a later layer fails and rows nobody downloaded by then are gone -- for the one caller that reads them, threaded through as `host_kv_authoritative` on private `forward_hidden_batch_inner` / `forward_batch_last_inner`. The contiguous path is byte-for-byte unchanged and still skips the download. FIVE MORE MODEL FEATURES WERE FOUND MISSING BY THE SAME COPY, on top of the five this entry already records, and THREE OF THEM WERE WRONG ON CPU TOO: `attention_scale` (Gemma scales Q itself and asks for a score scale of 1.0), `post_attn_norm` and `post_ffn_norm` (Gemma-2 sandwich norms), gpt-oss `o_bias`, and `gpt_oss_ffn` in place of the generic router. Found by running Gemma-2-2B through paged KV with NO GPU involved and watching it answer differently from the same model with a contiguous cache. Pinned by `the_paged_path_keeps_every_per_layer_feature_the_contiguous_one_applies`, sabotage-checked one feature at a time. EVIDENCE. New hardware test `crates/frink-models/tests/paged_metal_parity.rs`: same greedy continuation twice in one process, once per cache, 64-token prompt, 24 tokens, identical on Llama-3.2-1B-Q4_K_M (dense), OLMoE-1B-7B-Q4_0 (MoE) and Gemma-2-2B-Q4_K_M (sliding window); five consecutive Metal runs green, and green under `FRINK_METAL=0` too. End to end through the server on Llama-3.2-3B-Q4_K_M with FRINK_PAGED_KV_BLOCKS on Metal, paged and contiguous return the SAME text on three prompts, including this entry's own `Blue and red.`. `frink verify --backend metal --prompt-tokens 64` still OK on all three (that tool only exercises the CONTIGUOUS cache, so it is evidence the fix did not regress the fast path, not evidence about paging -- which is why the hardware test exists). NOT COVERED: gpt-oss end to end, because no gpt-oss checkpoint is on this host; its two paged features are held by the unit test above only. ALSO FOUND, NOT FIXED, NOT MINE: two checkpoints loaded into ONE process do not answer the same as either alone on Metal -- loading Llama-3.2-1B and then OLMoE gave three different OLMoE continuations across three runs, none the stable answer OLMoE gives alone, while paged and contiguous agreed with each other every time. Process-global Metal state surviving a dropped `Decoder`, most likely the host-pointer-keyed resident weight cache. The parity test runs one model per process because of it. THE SAME ZERO FILL WAS BREAKING A SHIPPED FEATURE WITH NO REFUSAL IN FRONT OF IT, found while checking how far the cause reached and fixed here rather than left running: `FRINK_PREFIX_CACHE_ENTRIES` stores the HOST rows as a snapshot, so on Metal it stored zeros, and the next request that restored the snapshot answered nonsense at full speed. Reproduced on Llama-3.2-3B: first request 'Blue and red.', second request off the same 53-position prefix ' question mark of the day. The question of the day is a question of the day.' -- the same token soup as the paged symptom, from the same cause. `sync_metal_attn_kv_to_host` is called right before the store and CANNOT repair it: it appends past `seq_len`, which the placeholder fill has already advanced. Fixed by the same switch, paid for only when a prefix cache is configured. After: three requests, two hits, 106 positions reused, 'Blue and red.' every time, and a multi-turn partial hit answers 'Paris'. NOT AFFECTED, checked rather than assumed: `/v1/embeddings` reads the returned hidden states and not the caches, and the continuous batcher prefills with `forward_token` (which leaves the host cache SHORT, not zeroed, and so is repairable by the existing catch-up) -- whether the batcher actually calls that catch-up before `forward_multi_seq` is a separate question and a separate defect, left for the wave that owns the batcher. WHAT REMAINS for the radix work is unchanged and lives in `wire-radix-prefix-cache`."
    status: completed
  - id: wire-radix-prefix-cache
    content: "STILL GATED, but ONE OF ITS THREE FINDINGS IS FIXED INDEPENDENTLY, because it needed no paged KV at all. The entry catalogued three defects in `frink-models::prefix_cache`: it is not a trie; `find_longest_prefix` is an O(n) linear scan; and `store` evicts index 0 -- FIFO, despite the module calling itself LRU. THE THIRD WAS A REAL SERVING BUG AND IS NOW FIXED. Verified rather than assumed: `find_longest_prefix` took `&mut self` for its counters and never reordered anything, so nothing on the hit path recorded that an entry had been USED, and `store` dropped the oldest ARRIVAL. That inverts the cache's whole purpose -- a shared system prompt is the oldest entry precisely BECAUSE it is the most reused, so it was the first thing evicted, and the next request off it recomputed the lot. Now a hit stamps a monotonic `last_used` and eviction takes the minimum, the same shape `frink_core::expert_store` uses; the O(n) eviction scan costs nothing because the lookup it follows already scans the same vector linearly. Three tests, and the first was CHECKED BY REVERTING the fix: `a_prefix_that_keeps_being_hit_survives_newer_arrivals` fails against the old `entries.remove(0)` and passes against the new one, so it is evidence rather than decoration. The module doc no longer claims LRU falsely. WHAT REMAINS GATED, unchanged: the linear scan and, more importantly, the per-entry `Vec<KvCache>` CLONE -- N conversations off one system prompt still hold N copies of its KV rather than sharing pages. That is the radix cache's job, and it needs a serving path that reads paged KV (paged-decode-path step 2) before it can be wired. Acceptance still names two prompts sharing a 2000-token system prompt holding ONE copy, reported as a byte figure"
    status: pending
  - id: radix-on-the-batcher
    content: "DONE, and the prerequisite it needed was never written down: the batcher had to run on paged KV first. The exclusivity was not protecting a deep hazard -- it was that a batched row could do neither pool acquisition nor prefix restore, so a KV pool or prefix cache forced the private path. A row holding a `PagedLease` gets both for free: pool-accounted by construction, and prefix-shared through the radix tree. THE DECODER HALF came first. `forward_multi_seq` batches the projections across sequences but must attend PER sequence, and that per-sequence step is the ONLY place the batched path touches a cache -- so paging it is a `MultiSeqKv` parameter on one body, not a second copy of a 300-line function. The contiguous arm keeps its three specialised kernels; the paged arm reaches `causal_gqa_attention_paged_sinks`, which covers all three in one entry point. Pinned BIT-identical across plain, windowed and scaled-plus-softcapped configs with three sequences at different positions and lengths, and sabotage-checked twice: dropping the window in the paged arm fails it, and so does dropping the softcap. THE SERVER HALF: a row's KV is a `RowKv`, and a paged row holds the WHOLE lease rather than just its caches, because the lease is what returns the page groups and every way a row can end -- finished, cancelled, evicted, refused at validation -- goes through dropping it. The batched step takes the caches out with `mem::take` and puts them back, which leaves the lease in the row so the groups stay accounted; `j` indexes `active` and only one backing vector is populated, which is asserted rather than assumed since a mixed batch would silently pair a row with another row's KV. Admission refuses a row the store cannot hold even when the block budget said yes: the two counts are meant to agree and the store is the one holding real memory. THE REFUSAL IS GONE rather than reworded, as the acceptance asked. `continuous_batching_composes_with_paged_kv` runs two concurrent rows over a shared store and demands the SAME IDS as two sequential private-loop generates, then asserts every page came back -- token-for-token, because the point is that turning two independent switches on does not change what the model says. Sabotage-checked: routing paged rows through the contiguous batched call fails it, and so does prefilling a paged row with the contiguous forward. STILL EXCLUSIVE, deliberately: the CONTIGUOUS pool and prefix cache keep the private path, because a batched row has no way to restore a `Vec<KvCache>` snapshot"
    status: completed
  - id: chat-template-kwargs-plumbing
    content: "DONE, and it was bigger than this entry said. The premise was that the template layer already accepted the passthrough and only the server never sent it. Half true: `frink_models::chat_template` compiles and evaluates the checkpoint's real Jinja source, and it HAD NO CALLER AT ALL. `frink-server` and `frink-cli` were each still running their own copy of the marker sniffer -- six hand-written renderers picked by which literal marker the template string happened to contain -- which is why a Mistral checkpoint was being served `user: hi`, `[INST] … [/INST]` matching none of them. So there was nothing to pass kwargs TO. Both binaries now render through the evaluator, and both hand-written copies are deleted rather than deprecated. On top of that: `chat_template_kwargs` and `reasoning_effort` on the request; tools described BY the template when it really consumes them -- established by rendering with and without a tool, not by looking for the word, because a false positive there silently skips the preamble and offers the model tools nobody described (structurally, with a replayed call's `arguments` parsed back to an object, because a template written against HuggingFace's apply_chat_template gets a dict and `| tojson` on the wire string would double-encode it) and by the text preamble when it does not; the effort vocabulary probed ONCE at load rather than per request. `force_reasoning` is no longer hardcoded false -- and it is not derived from the resolved thinking mode either, which would have been a guess: `ReasoningFormat::prompt_opens_reasoning` reads it off the prompt that was actually rendered, because the same checkpoint opens the block or does not depending on what the kwargs rendered. A template that will not compile now FAILS THE REQUEST with the compiler's message instead of falling back to a guessed framing, which is the models module's stated design and the whole point. Acceptance met: `an_off_vocabulary_reasoning_effort_is_quantized_rather_than_interpolated` (minimal against a low/medium/high grader renders `E:low`), plus the drop case, the precedence case, tools-force-thinking, and a prompt-opens-the-block case that reads the same generated text two ways depending on the prompt behind it"
    status: completed
  - id: think-gears-on-models
    content: "DONE. `frink_edge::derive_think_gears` + `ThinkGears`, and `/v1/models` now carries `supported_reasoning_efforts` and `default_reasoning_effort`. The build order is the whole content of the function and the test is an EQUALITY on the list rather than a membership check: `off` if the template can be turned off, `adaptive` if it has that third state, then the effort ladder ascending by scale -- or a bare `on` when there is a toggle but no ladder, since `off` needs a counterpart to be worth offering. Default: whatever the bare render already matches (`off`/`adaptive`), else the probed effort default, else `medium` if on offer, else the last gear -- and it is ALWAYS a member of the advertised list, which is its own test, because a default naming a gear a client cannot ask for is worse than none. Acceptance met: a checkpoint that says nothing about thinking carries NEITHER field, not an empty list -- `a_checkpoint_with_no_thinking_controls_advertises_neither_field`. The profile this reads was already being probed at load by chat-template-kwargs-plumbing, so the wiring was `probe_thinking_profile` over the same render closure the effort probe uses"
    status: completed
  - id: streaming-tool-call-deltas
    content: "DONE. The chat stream now runs `frink_edge::ToolCallParser` incrementally instead of calling `parse_output` on the full text at the end. `overlap` was `!tools_active && batcher.is_none()` -- tools disabled incremental streaming entirely, because finding a call needed the whole text -- and is now just `batcher.is_none()`, since the parser is incremental. Wire shape is OpenAI's: `ToolCallDelta` with `index`, and identity only on the first delta of a call; a buffered path expresses a whole call as a delta with every field set, so there is ONE type on the wire rather than two. `CallEnd` deliberately emits nothing -- every byte of the arguments already went out as a fragment, and repeating them makes a concatenating client produce them twice. Truncation beats a call: a generation that ends on `length` reports `length` even if it opened one, because a half-written call must not be executed. FOUND AND FIXED A REAL PARSER BUG WHILE TESTING THIS, in frink-edge rather than the server: an invoke grammar closes twice (`</function>` then `</tool_call>`) and only the first belongs to the call, so the block close was released as CONTENT -- raw markup printed into an answer. At one character per chunk it did not even arrive as a strippable whole, it dribbled out as `<`, `/`, `t`. Fixed by withholding closing markers as well as opening ones, with a regression test at three chunk widths that fails against the previous code. Acceptance met: a 4 KB argument arrives in many deltas whose concatenation parses, asserted in `a_streamed_call_opens_then_delivers_its_arguments_in_pieces`. STILL OWED: the continuous-batching path still emits whole calls (it has no incremental stream to ride on), and the JSON-payload families still arrive whole by design"
    status: completed
  - id: cache-status-and-rebuild
    content: "DONE for the surface. `crates/frink-server/src/cache_admin.rs`, routed at `GET /v1/cache/status`, `POST /v1/cache/rebuild` and `POST /admin/prepare_stop`. Status renders through `frink_edge::cache_report`, which drops a column nothing can be said about rather than zero-filling it. Rebuild runs gate -> arithmetic -> pool, in that order, and EVERY refusal reopens the gate, so a refused rebuild leaves the engine serving exactly what it was serving; `KvBlockPool::resize` refuses to shrink below what in-flight requests hold and names the floor rather than making the caller find it by being rejected again; a KV-side resize invalidates the prefix cache but keeps its lifetime counters, because a stored prefix names positions in an allocation that has stopped existing. The gate is now LOAD-BEARING and not just recorded: `check_admission` runs ahead of validation on `/v1/chat/completions`, `/v1/completions`, `/v1/responses` and `/v1/messages`, so a request arriving mid-rebuild is refused with a 503 naming which of the four closed states it hit. `MaintenanceGate::serving()` exists for exactly this reason -- the server builds its state AFTER the startup checkpoint is activated, and a gate started in `Loading` there would refuse every request forever, since nothing would call `finish_loading`. STILL OWED: sizing the pools with `plan_cache_budget` at LOAD (today they are sized from the flags), and the full transaction in runtime-cache-rebuild"
    status: completed
  - id: stats-and-request-ring
    content: "DONE. `frink-edge`'s `stats.rs` (request ring with an all-time cursor, nearest-rank p95, TTFT averaged only over rows that HAVE one, and a sliding-window rate divided by the OBSERVED span so a quiet server reports 0 rather than its lifetime mean) and `maintenance.rs` (the prepare-stop barrier), served at `GET /v1/stats`, `GET /v1/requests` and `POST /admin/prepare_stop`. The acceptance case is pinned: a stop with an in-flight request that never terminates leaves the server `Stopping` and UNSEALED -- `seal` refuses, snapshots nothing, and does not reopen admission -- and sealing is idempotent for the life of the process, so a supervisor whose response was lost retries and gets the same totals rather than a second, larger measurement. The durable outbox half stays open under receipt-durability"
    status: completed
  - id: wire-edge-scheduler
    content: "RESOLVED, AND THE ENTRY'S PREMISE WAS WRONG -- checked rather than assumed. It claimed the two 'hold the same policy twice'. They do not, and the evidence is in both files. `frink-server::batch_scheduler` admits against the BLOCK model frink ships: `ceil((prompt + max_tokens) / block_size)` blocks for the request's lifetime. It contains ZERO sliding-window logic -- all fourteen `swa|window` matches in it are `swap_remove` -- and it says so in its own words at line 1088: 'frink serves no windowed or recurrent model through this batcher.' `frink_edge::scheduler::PrefillPass` admits against the WINDOW and RECURRENT models: chunks that must end on a page boundary, a request reclaiming its own slid-out window, a recurrent slot per request. Those are exactly the cases the batcher excludes. So they are not two implementations of one policy; they are one implementation each of two different ones, for two different memory models. THE GENUINE OVERLAP is 'FIFO, bounded by a token budget' -- a `min()` and a subtraction, plus a doc comment both happen to phrase the same way ('FIFO cannot starve, so FIFO it is'). Routing the LIVE batcher through a module that has never run, to deduplicate two lines, would put an unexercised path on the serving hot path: a large risk for no benefit. DECISION: the batcher is authoritative for the block model, `PrefillPass` for the window and recurrent models, and NEITHER grows the other's rules. Made structural rather than left in a plan file: both modules now carry a cross-reference stating the split and pointing at the other, so the next reader neither merges them nor re-implements one inside the other. `PrefillPass` still has no caller -- correctly, since frink serves no window model yet; it is the admission half that `swa-paged-allocator`, `window_pool` and `anchor::decode_slide` are all built toward, and deleting it would throw away the piece those three are waiting for"
    status: completed
  - id: window-slide-during-decode
    content: "DONE. The half that was missing was an ENGINE, and it is now on both serving paths. `PagedLease` (crates/frink-server/src/generate.rs) holds `groups: Vec<Option<PageGroup>>` plus a private `spare` list, and `before_step` runs `anchor::decode_slide` on every decode step of the private `generate` loop and of the continuous batcher's batched step -- so a window model now holds its PROMPT PLUS A WINDOW rather than its whole context. ADMISSION IS SIZED BY THAT, which is the part an operator can actually see: `paged_hold_positions` prices a windowed request at `prompt + 2*(window + SWA_RETAIN_GAP) + eviction_interval + 2*page`, the ceiling frink-edge's own bound test already asserted at every step of a 100_000-token run, and the SAME function feeds the batch scheduler's block budget -- one formula, because a budget pricing the whole context would keep refusing admissions the store would happily serve. Acceptance is stated as the only observable thing: `a_window_model_runs_on_a_store_too_small_for_its_whole_context` serves a 400-token generation on a 60-block store and shows the SAME store refusing the same request on a full-attention model. RECYCLING RATHER THAN RELEASING, and that is the load-bearing design choice: a slid group goes to the request's own `spare` list, not back to the store, because handing it back would make `push` fallible again mid-answer with nowhere to report it (`sample_until_stop` takes a closure returning `Vec<f32>`). The generosity happens ONCE, at admission, where a refusal can still be returned -- which is the larger saving anyway. `PagedKvCache::append_block` is the core primitive: the block table keeps its absolute-position indexing and names one physical block at two indices, the stale one nothing reads and the live one, and `release` was made to hand each DISTINCT block back once because per-index would put a recycled id on the free list twice. THREE RULES THE TESTS PIN, each a silent wrong answer rather than a crash. (1) `ModelConfig::uniform_sliding_window` is a NEW question, opposite to `kv_block_window`: that one asks 'does any layer constrain the block layout' so one sliding layer is enough, this asks 'may a page behind the window be taken away' and there one FULL-ATTENTION layer is enough to say no -- a page group holds one block in every layer, so an alternating-SWA model (gpt-oss, Gemma-3) does not slide at all, and `a_window_model_runs_on_a_store_too_small_for_its_whole_context` refuses it alongside the full-attention model. (2) The slide floors at the radix tree's locked prefix, never at zero: those pages are shared and freeing them takes them out from under every other holder. (3) A SLID SEQUENCE IS NOT PUBLISHED to the tree -- the tree keys on a PREFIX and a slid sequence's prefix is precisely what it gave away; what it still holds is a suffix, and publishing would hand the next request pages whose contents belong a thousand positions later, matched on and wrong. THE ANCHOR IS WIRED TOO, so `anchor::AnchorState` is no longer a policy without a caller: `resolve_anchor_token` runs ONCE at load against the served checkpoint's own family opener and its own tokenizer, `observe_sampled` offers each sampled id on the hot path for the cost of one comparison, and a dropped anchor is CLEARED so a later call in the same turn can take another -- which is what `a_tool_call_anchor_holds_the_window_back_and_then_lets_go` pins in three steps (holds, lets go, re-anchors). SABOTAGE RESULTS, including two that taught something. `every_page_the_kernel_still_reads_is_one_the_lease_still_owns` exists because the token-identity test COULD NOT catch an over-free: the admission slack means a recycled page is not physically reused until long after the window has left it, so freeing a whole window too much still produced the right answer on the fixture -- robust, but not evidence, so the invariant is asserted where it is true (every in-window table index has a live group) rather than where it happens to show. And the `has_slid` publish guard was initially redundant with a `break` on the first hole; the loop now `expect`s instead, so removing the guard fails. Caught: no slide at all (6 tests), no spare reuse (4), admission ignoring the window (5), freeing two pages past the policy (1), publishing a slid sequence (1), `kv_block_window` in place of `uniform_sliding_window` (1), `Drop` forgetting the spares (1), no slide in the BATCHED step (1), the budget pricing the whole context (1), and all three anchor sabotages. NOT DONE, deliberately: PREFILL does not slide. `forward_batch_last_paged` materialises positions `0..prompt_len` through `PagedKvCache::to_contiguous` to reuse the ONE prefill kernel rather than maintaining a second blocked one, so every prompt page must be live while it runs -- which is why the bound has a `prompt_len` term at all. `anchor::prefill_slide` therefore still has no caller. Closing that means either a windowed gather (positions shift, so the window arithmetic shifts with them) or the second kernel the gathering approach was chosen to avoid; it is worth its own entry, not a tail-end addition here. `CacheManager` still has no caller either -- the slide reached serving through `PagedLease` rather than through it. AND THE SCOPE THIS INHERITS, which bounds the word 'serving' above: every test here is a CPU test. Paged KV's GPU refusal was lifted on main while this branch was in flight -- a Metal prefill was leaving K/V on the device and filling the host cache with placeholders that the paged prefill copied into the page store -- and its `paged_metal_parity` hardware test covers a sliding-window model, but that test predates the SLIDE and so says nothing about it. The window slide has never run on a GPU"
    status: completed
  - id: persistent-gpu-expert-cache
    content: "HALF LANDED, and the half that landed is the half that was testable. THE FINDING FIRST: the plan's premise -- `run_expert_placed` 're-uploads each weight matrix from scratch every call' -- is STALE for CUDA. `frink_cuda::resident_cuda_weights` (crates/frink-cuda/src/gpu.rs) already caches device buffers keyed by (host_ptr, len), and Metal's `resident_weight_buffer` does the same with a fingerprint guard. So residency EXISTS. What does not exist is BOUNDED residency governed by a policy: the CUDA map has no budget and no eviction at all, so a real MoE grows it until cudaMalloc fails, and Metal's budget overflow response is `cache.clear()` -- dropping the hot experts along with the cold. And neither is connected to `frink_edge::ExpertCache`, which VERIFIED HAS NO CALLER anywhere in frink-server or frink-cli: it plans CopyPlan / GatherPlan / BankEntry and nothing on earth executes them. That, precisely, is what 'q* is inert' means -- not that the split is wrong, but that the split's output is never read. LANDED: `frink_edge::expert_slots` (17 tests), the executor those plans never had. `ExpertSlots` owns a bounded slot pool of `slots x row_bytes` PER BANK (gate/up/down get their own widths: a checkpoint may quantize them differently, so one width for all three is wrong for at least one), applies a CopyPlan's `(dst_slot, src_row)` pairs against layer-local host banks via the `ExpertRows` trait, and counts what crossed the link. Device memory stays behind the `SlotDevice` trait so frink-edge is STILL tensor-free and every rule is testable on a host with no GPU; `HostSlotMemory` is a real implementation of it (a CPU-only build's pool IS host memory and its link IS a memcpy), not a mock. FIVE RULES CARRY IT, each guarding a silent-wrong-answer rather than a crash: a plan is validated WHOLE before a byte moves (the cache has already recorded the residency by the time the caller holds the plan, so a half-applied plan leaves the cache pointing at another expert's bytes -- that does not fail, it multiplies); halves of unequal length are refused (pair i would not be a pair); a slot written twice in one plan is refused (the second wins and the first expert is absent from the slot the cache promised it in); a host row is checked for EXACT width, since a short row leaves the slot's tail holding the previous occupant -- an expert spliced from two, which emits plausible tokens; and a gather from a slot the pool knows nothing about is refused rather than laundering a failed copy into a second slot both then record as valid. FAILURE IS ATTRIBUTED BY WHAT THE BACKEND CAN KNOW: a refused copy costs its own slot (`SlotFault::Device`), a failed FLUSH costs every slot the plan wrote (`SlotFault::DeviceFlush`), because a backend that batches transfers cannot say which landed -- blaming only the last would leave the rest reading back as resident. TWO EXISTING DEAD THINGS BECAME REACHABLE, which is the evidence this is the executor those APIs were written for: `BankResidency::copy_route` and `ResidencyError::SlotRemapOnUnpinnedLayer` had no caller, because an LRU slot remap needs a device alias for host rows that an unpinned layer does not have -- `ExpertSlots` is what finally asks. ADDED: `ExpertCache::forget_slot`, the planner half of the fault path; it drops the slot AND stamps its LRU usage at i64::MIN so the re-fetch reclaims that very slot rather than evicting one that really holds an expert. THE CUDA BACKEND LANDED TOO, and is labelled for exactly what it is: `frink_cuda::expert_pool::CudaExpertPool` implements `SlotDevice` against real cudarc calls, COMPILE-VERIFIED ONLY -- no benchmark host has run it, and its end-to-end test is `#[ignore]`d in the crate's existing hardware-test convention. Written out rather than stubbed (unlike `measure_pcie_gather`, which stayed a stub) because its correctness is mostly the type system's to check -- an allocation per slot, a bounds-checked index, a driver copy -- where a timing loop would have put a number nobody measured into a profile. ONE SLOT PER ALLOCATION rather than one buffer per bank, and not for taste: a slot-to-slot copy needs the source borrowed shared and the destination mutably AT THE SAME TIME, which two sub-views of one `CudaSlice` cannot give and `split_at_mut` over separate allocations can -- so the gather path is ordinary safe code instead of raw driver pointer arithmetic written against hardware nobody here can run. The index arithmetic of that split is the one line a compiler cannot check, so `split_pair` sits OUTSIDE the `cuda` feature gate and is tested by the ordinary GPU-less `cargo test --workspace` run. Also found by reading cudarc rather than assuming: `htod_sync_copy_into` and `dtod_copy` `assert_eq!` their lengths -- a PANIC, which mid-decode takes the server down -- so the host-side write converts a mismatch into an error, while the device-to-device copy does not need to, its equality being a property of construction. Structural consequence recorded in publish-order-needs-frink-edge: frink-cuda now depends on frink-edge, tightening the required publish position from 'before frink-core' to 'before frink-cuda'. ACCEPTANCE, HONESTLY SPLIT. 'A decode step that hits the expert cache issues ZERO weight uploads, shown as a counter' is PROVEN on a host pool, by `a_step_that_hits_the_cache_copies_nothing`: ten warm steps move `SlotStats::host_bytes` not at all. What is NOT done and cannot be closed here: nothing in the decode path calls any of this, `ExpertCacheStats::miss_rate` is still not on `/v1/stats`, and the CUDA pool has never executed. The rest of the acceptance needs a real MoE checkpoint on a real GPU -- the NAMED benchmark host parity-scope-decision still owes"
    status: pending
  - id: concurrent-cpu-moe-executor
    content: "THE OTHER HALF OF THE SAME BLOCKER. q* assumes a step costs max(fetch, cpu). Frink's CPU MoE runs synchronously inside the forward, so it costs fetch + cpu and the optimal split is trivially 'fetch everything' -- i.e. `qstar` cannot be wrong because it cannot matter. FreeToken's executor is a pinned-buffer worker pool with a doorbell handshake, one thread per PHYSICAL core (SMT siblings share load ports and buy nothing), and it reserves the last core for the coordinator. It also documents why the obvious implementation is wrong: a spin-wait kernel pinned reported GPU utilisation at 99% and laptop power schedulers responded by clamping CPU frequency, a NET DECODE REGRESSION. Gated on persistent-gpu-expert-cache. Acceptance: a measured A/B on one MoE model showing hybrid decode beating pure offload on a host where `frink bench bw` recommends hybrid, and matching it where it does not"
    status: pending
  - id: bench-bw
    content: "PARTIAL, and precisely so. The WRITER exists: `bench_profile::entry_from` derives the profile entry and its verdict, `write_profile` lands it atomically (temp file plus rename, because a reader that catches a half-written profile gets serde's parse error, which `read_profile` turns into NO profile -- silently discarding a measurement the user did take), and `frink bench-bw` is the command. The reader had been complete for a while; this closes the loop. TWO REFUSALS carry the design. One side measured is `NotMeasurable::OnlyOneSide` and writes NOTHING: the fetch fraction is a RATIO, so one number implies nothing about the split, and a half-profile is worse than none because `policy_for` would consult it as though it were whole. And an UNOPTIMIZED build refuses too -- a debug binary measures its own code generation, and because the device copy is driver-performed and unaffected it moves the RATIO rather than merely lowering both numbers, so a verdict that flips with `--release` measures nothing; `--allow-debug-build` is the escape hatch and has to be asked for by name. The verdict is derived from the CONTENDED pair wherever it exists, so it cannot disagree with `fetch_fraction` about which numbers it came from. STILL OWED, and it needs the named benchmark host parity-scope-decision asks for: `measure_pcie_gather` is a documented stub rather than a fabricated timing loop -- what it must do is a pinned-host-to-device copy timed with CUDA EVENTS rather than a wall clock, best of N, then the same again while the CPU stream runs on the other cores to fill the contended pair. Writing a timing loop nobody can run would put a number in a profile that no measurement stands behind, which is the one thing this command exists to prevent. Acceptance stays open until a real card produces a profile that `policy_for` turns into a non-default `QStarPolicy`"
    status: pending
  - id: double-buffered-prefill
    content: "Not ported and not portable as policy alone: it is a copy stream, two events per buffer, and a fence. A prefill touches every expert, so streaming misses one at a time is pointless -- the layer is copied whole into a borrowed two-layer slot region while the previous layer computes, with position == expert id so routing ids index the buffer directly. The invalidation rule is the subtle part: the borrowed slots' previous owners must lose their residency, or the next decode hits on a slot holding someone else's bytes. `frink_edge::ExpertCache::materialize_layer` already implements exactly that bookkeeping and is tested for it; what is missing is the stream. Gated on persistent-gpu-expert-cache. Acceptance: prefill on a MoE model overlaps its expert copies with compute, shown as a wall-clock A/B against the serialized path"
    status: pending
  - id: expert-bank-quant-formats
    content: "DECIDED: WON'T DO, which is what the acceptance asked for -- 'a written decision, not an implementation' -- and it is the SAME decision already recorded for ftw-format and df11-lossless-codec, as this entry predicted it would be ('If the answer is that frink stays GGUF-native, say so here and close this, because it also closes ftw-format'). FRINK STAYS GGUF-NATIVE. MXFP4 is already in frink-quant as a GGUF-style fused dequant+dot, because GGUF carries it; NVFP4, FP8-block and ds_fp4 are safetensors-era bank formats with no GGUF representation, so adding them means adding a container to put them in, which is exactly what ftw-format was closed for. THE PART WORTH KEEPING is the observation this entry makes about the bank ORDER being an ABI -- every copy path and kernel dispatch iterates it. That constraint is real and it now has a home regardless of these formats: `frink_edge::expert_slots::SlotGeometry` carries `row_bytes` PER BANK precisely because banks are not interchangeable (gate and up are `[ffn, hidden]` while down is `[hidden, ffn]`, and a checkpoint may quantize them differently), and `ExpertSlots` refuses a row that is not exactly one slot wide rather than truncating it. So the ABI lesson is ported even though the formats are not. REVISIT IF frink ever grows a non-GGUF weight path for a reason of its own; these formats would then be cheap to add, and until then they are a container decision wearing a quantization costume"
    status: completed
  - id: ftw-format
    content: "DECIDED: WON'T DO the format; ITS REAL WIN IS ALREADY PORTED, which is what made the decision easy rather than a judgement call. The item itself named where the value is: 'its real win is not the format but the loading discipline around it -- pin-after-fill ... plus mlock with a sticky downgrade to pageable on failure.' Both are in frink already, and were before this decision was written. `frink_edge::residency::SettleAction`'s doc comment states pin-after-fill verbatim as a rule the loader must follow: 'Always *after* the fill: the banks are lazy anonymous mappings, so page-locking an empty one faults and zero-fills every page, and the read then overwrites all of it -- a whole redundant pass over hundreds of gigabytes.' The sticky downgrade is `ResidencyPlan::record_lock` plus `lock_quota_exhausted`, built on the observation that the first `RLIMIT_MEMLOCK` refusal means every later, larger request fails too. WHAT IS LEFT OF FTW once those are removed is the container: 4096-aligned, padded tensors so any tensor or shard-local slice is O_DIRECT-legal, plus an index JSON. That alignment exists to make DIRECT READS legal, and frink does not do direct reads -- it mmaps, so the kernel's page cache and readahead do that job and the alignment property has no consumer. Adding a third container (after GGUF and safetensors) to gain an alignment guarantee nothing consumes is cost with no matching benefit. REVISIT IF frink ever adds an O_DIRECT load path for checkpoints too large for the page cache to help -- at which point the alignment requirement becomes real and this becomes worth re-costing. Gated on expert-bank-quant-formats as the item said; that gate resolves the same way, since staying GGUF-native is what makes both won't-do"
    status: completed
  - id: dsv4-cost-model
    content: "DONE. `crates/frink-edge/src/dsv4.rs`, 34 tests, no tensors. Per-page cost for the compressed/window/indexer tiers, the ceil-to-bytes-per-token unit costs the cache-status sliders read, `dsv4_pool_sizes`/`dsv4_pool_bytes`, and the binary search for the largest page count a budget buys. Both traps are handled explicitly: sizing rounds half-to-EVEN (sharing `placement::round_half_even` rather than keeping a second copy free to drift) while the unit costs ceil, and the reserved-window-pages code was ported over its own contradictory docstring. Still gated on a real DeepSeek-V4 checkpoint for validation -- the arithmetic is pinned by tests, the checkpoint is not"
    status: completed
  - id: remaining-parser-families
    content: "DONE, and the entry was STALE in an instructive way: both grammars were already implemented. `ToolCallFormat::MiniMaxM3` carries the namespaced recursive element grammar (`parse_m3` / `parse_m3_block`, the `]<]minimax[>[` constants) and `ToolCallFormat::MuseGlimmer` carries the ATEM channel format on both sides (`Markers` with `<atem:function_calls>` / `<atem:invoke` / `<atem:parameter`, plus `replay_streaming` for the channel layer). What was genuinely missing was the ACCEPTANCE: the cross-family streaming matrix covered four invoke families and not these two, so two implemented parsers had no evidence they streamed correctly. Both are now in `streamed_fragments_concatenate_to_the_final_arguments` and `streaming_agrees_with_one_shot_for_every_invoke_family`, at every chunk width including ONE CHARACTER -- which for M3 means a namespace prefix split across thirteen pushes. TWO THINGS THE FIXTURES HAD TO GET RIGHT, and getting either wrong makes the test pass against the wrong protocol. M3 names a parameter by its own ELEMENT (`]<]minimax[>[<city>Rome]<]minimax[>[</city>`), not by an attribute -- it is a different protocol from MiniMax-M2, not renamed tags. And the ATEM block DOES NOT STAND ALONE: muse-glimmer's channel layer classifies the segment, so the block must arrive inside a `to=tool` message (`assistant to=tool<|message|>...<|eot|>`). A bare `<atem:function_calls>` block is correctly parsed as TEXT, which is what the first version of this fixture proved by failing. Original gate was 'do them when a checkpoint from either family is in docs/MODELS.md' -- superseded, since the code was already there and only the proof was missing"
    status: completed
  - id: real-moe-checkpoints
    content: "FreeToken serves 20+ MoE checkpoints for real. CLAUDE.md:89: 'Presets glm_5_2 / deepseek_v4_pro / kimi_k3 are sketches, not proof of real-checkpoint support.' No amount of policy parity substitutes for this, and several items above (dsv4-cost-model, window-slide-during-decode, persistent-gpu-expert-cache) cannot be validated without one. Acceptance: at least one frontier MoE checkpoint runs end to end with a verified greedy-id parity receipt, which is a prerequisite for calling ANY of Phase C done"
    status: pending
isProject: false
---

# FreeToken feature parity, what the port left, and in what order

> Written **2026-08-25** on branch
> `claude/freetoken-rust-conversion-23u6ng`, from the FreeToken source
> at `FlashML-org/FreeToken@main` and a read of frink's own tree. It is
> the follow-on to the port that landed on that branch: four commits
> adding `crates/frink-edge` (193 tests) and wiring its two output
> parsers into `frink-server`.
>
> Every frink claim below carries a `file:line`. Every FreeToken claim
> comes from its Python source, not its README.
>
> **The paper was not readable from the environment this was written
> in**, `arxiv.org` and every mirror tried are blocked by the egress
> proxy. The abstract and the `q*` framing come from search results; the
> algorithms come from the source, which is the ground truth for a port
> anyway. Nothing here is sourced from the PDF.

## What is already done

`crates/frink-edge` is the host-side **decision** logic: the arithmetic
that decides what to compute where, and the state machines that turn a
token stream back into an agent-shaped response. It is deliberately
tensor-free, so all 193 of its tests run on any host with no GPU and no
model.

| Module | State |
|---|---|
| `parser::{reasoning,tool_call}` | **wired**, `reasoning_content` and nine tool-call formats on `/v1/chat/completions`, streaming and buffered |
| `detokenize::stop_prefix_holdback` | **wired**, `frink-server::stop` delegates to it, so the workspace has one implementation of the withhold rule |
| `radix::{plain,swa,hybrid}` | tested, no caller |
| `qstar`, `expert_cache` | tested, no caller |
| `pool`, `placement` | tested, no caller |
| `scheduler` | tested, duplicated by `frink-server::batch_scheduler` |
| `effort`, `cache_report` | tested, no caller |

## The blocker that ranks everything

FreeToken's central claim is that a decode step's expert-cache misses
can go two ways at once, fetched over PCIe *while* the CPU computes
others, so the step costs `max(fetch, cpu)` and the split that
minimises that maximum is a property of the machine.

Frink cannot make that true yet, for two independent reasons:

1. **There is no persistent GPU expert residency.**
   `crates/frink-moe/src/lib.rs:878`, in its own doc comment: *"Every
   call re-uploads each weight matrix to the device from scratch […] not
   yet the persistent-GPU-residency throughput win real expert offload
   needs."* An expert cache with nowhere to cache an expert is a
   bookkeeping exercise.
2. **The CPU MoE path is synchronous.** It runs inside the forward, so
   the two sides serialise and the step costs `fetch + cpu`. Under that
   cost model the optimal split is always "fetch everything", which
   means `qstar` is not merely unused, it *cannot matter*.

Both are in Phase C. Neither is a policy problem. This is why Phase A
comes first: **Phase A pays off on CPU and Metal today**, on the
hardware frink actually has receipts for, and needs no CUDA at all.

## Phase A, prefix reuse and agentic caching (no GPU)

The cheap half of FreeToken, and the half frink is furthest behind on
in a way users would feel.

What exists today is not a radix cache and says so:
`crates/frink-models/src/prefix_cache.rs:16`, *"not a trie/radix-tree
structure"*. `find_longest_prefix` is an O(n) linear scan (`:76`),
`store` evicts index 0 (`:131`) despite the module calling it LRU, and
each entry clones a whole `Vec<KvCache>`. A thousand requests off one
system prompt hold a thousand copies of its KV.

Order: `anchor-checkpoints` → `cache-manager` → `swa-paged-allocator` →
`wire-radix-prefix-cache` → `radix-on-the-batcher` →
`window-slide-during-decode`.

**Phase A is closed.** All six landed in that order, and the last one
is where the chain pays off: the anchor policy written first now has a
caller, because the window it holds back is a real serving path's.

`anchor-checkpoints` is first because it is the largest thing the port
left, it is pure logic, and `frink-edge` already *pays* for it: the
window pool floor has an `anchor_checkpoints` term
(`crates/frink-edge/src/pool.rs:368`) sizing a pool for a feature that
does not exist.

## Phase B, the agent-facing API (no GPU)

Smaller, independent, and each item is worth doing alone. The one with
the widest blast radius was `chat-template-kwargs-plumbing`, and it hit
wider than expected: the Jinja evaluator in
`crates/frink-models/src/chat_template.rs` had no caller, so both
`frink-server` and `frink-cli` were still sniffing the template for
literal markers and serving every unrecognised family
(Mistral, Phi, Yi, DeepSeek-R1) a framing it has never seen. Fixing the
passthrough meant fixing that first; both hand-written copies are now
deleted. `think-gears-on-models` is the natural next item, because the
effort profile it needs is already probed at load by the work above.

`radix-on-the-batcher` sits in Phase A but is felt here: continuous
batching is refused whenever a prefix cache is configured
(`crates/frink-server/src/lib.rs:1376`), so a deployment picks
concurrency *or* prefix reuse. That exclusivity is the current design's
honest answer to a real hazard, and page-granular sharing is what
removes it.

## Phase C, the compute half (CUDA, gated)

Do not start before `parity-scope-decision`. FreeToken is CUDA-only;
frink's CUDA bar is "must compile" with no benchmark host. Every item
here needs hardware nobody has run this engine on.

Order within the phase is forced: `persistent-gpu-expert-cache` →
`concurrent-cpu-moe-executor` → `bench-bw` → `double-buffered-prefill`.
The first two are what make `qstar` a real decision; the third is what
gives it real numbers; the fourth is a separate throughput win on the
prefill path.

CUDA graph capture is **not** a gap, `CudaDecodeGraph` exists at
`crates/frink-cuda/src/graph.rs:31`.

## Explicitly not ported, and not planned

- **FreeToken's Triton and CUDA kernels, its C++ CPU-MoE extension, its
  torch model stacks.** Frink has its own. A line-by-line port is a
  rewrite of frink, not a port into it.
- **Tensor parallel / multi-GPU.** FreeToken carries TP plumbing
  throughout its scheduler I/O; frink has none anywhere in `crates/`.
  It is on frink's own roadmap independently and does not belong to
  this plan.
- **The daemon, supervisor, proxy, `ft launch`, `ft shell` TUI.**
  Product surface around the engine, not the engine. `ft launch`'s one
  genuinely good idea, clearing cloud API keys from the child
  environment so an agent cannot silently fall back to a paid endpoint, is worth stealing on its own merits, and is not parity work.
- **`/v1/responses`.** Neither engine's users have asked for it here.

## Definition of done

There is no single "parity" line to cross, and pretending otherwise
would be the wrong shape for this plan. Instead:

- **Phase A done** = a second request against a shared system prompt
  reuses it without recomputation, on CPU and Metal, with a conservation
  test proving no page is leaked or double-freed, and an agentic turn
  that edits its context at a tool-call boundary keeps the prefix before
  the edit.
- **Phase B done** = a coding agent pointed at `frink-server` gets
  streamed tool-call arguments, `reasoning_content`, advertised thinking
  gears, and can resize the pools without a restart.
- **Phase C done** = on a named CUDA host, a decode step that hits the
  expert cache issues zero weight uploads, and hybrid beats pure offload
  on a machine where `frink bench bw` says it should. Not before
  `real-moe-checkpoints`.
